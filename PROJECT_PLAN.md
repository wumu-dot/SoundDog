# SoundDog — 工业异响音频检测节点

> *"Like a watchdog, but it listens."*
>
> 基于 STM32F407 的分布式工业音频异常检测系统
> 当前阶段：**传统 DSP（MFCC + 阈值）**，未来可升级 CMSIS-NN 轻量模型

---

## 1. 项目概述

### 1.1 一句话描述

一个可复用的、支持 RS485 工业总线组网的音频异常检测节点，通过 MFCC 特征提取 + 频谱比对，在 STM32F4 上实时判断设备运行声音是否异常并上报报警。

### 1.2 核心价值

| 维度 | 说明 |
|------|------|
| **可复用** | 框架通用，换传感器/换检测参数/换通信协议，核心代码不改 |
| **工业级** | RS485 差分总线，抗干扰，支持 32+ 节点 |
| **边缘检测** | 全部运算在 STM32 端完成，不依赖云端，延迟 < 50ms |
| **低成本** | 单节点 BOM 成本 < 80 元（含核心板+麦克风+RS485+报警） |
| **确定性** | 传统 DSP 算法，行为可预测、可解释、无训练不确定性 |

### 1.3 应用场景

- 电机/轴承/齿轮箱运行异响监测
- 风扇/压缩机/泵体状态监听
- 产线设备预防性维护
- 可扩展到振动、温度等多传感器融合

---

## 2. 技术路线：三阶段策略

**核心思路：先确定性地跑通，再逐步智能化。每一阶段的产出都直接喂给下一阶段。**

```
┌──────────────────────────────────────────────────────┐
│  Phase A: 传统 DSP（当前目标）                          │
│  MFCC + 特征向量 → 模版比对/阈值 → 正常/异常            │
│  产出: 能跑的检测节点 + 频谱可视化                       │
│  不确定性: 零。算法完全可解释。                          │
├──────────────────────────────────────────────────────┤
│  Phase B: 数据积累                                     │
│  用 Phase A 的节点采集标注样本（正常/异常音频片段）       │
│  产出: 几百段标注好的训练数据                            │
│  依赖: Phase A 跑通                                    │
├──────────────────────────────────────────────────────┤
│  Phase C: 模型升级（可选，未来）                         │
│  MFCC 特征 → CMSIS-NN 二分类 → int8 量化 → 部署         │
│  产出: 更聪明的检测模型                                  │
│  依赖: Phase B 积累的数据 + 特征提取代码直接复用          │
└──────────────────────────────────────────────────────┘
```

### 为什么先传统后 ML

| | Phase A — MFCC + 阈值 | Phase C — CMSIS-NN |
|--|----------------------|---------------------|
| **不确定性** | 零。FFT/MFCC 是确定性算法 | 依赖样本质量、训练策略、量化精度 |
| **调试** | 频谱直接显示到 OLED/串口 | 黑盒推理，难以定位问题 |
| **数据需求** | 不需要，凭直觉设阈值 | 几百段标注样本 |
| **兜底价值** | 即使不做 ML，本身就是完整系统 | 一旦翻车，什么都没有 |
| **代码复用** | MFCC 特征提取 → 后续直接当 ML 输入层 | — |

### 算力验证：F407 绰绰有余

| 模块 | CPU 占用 | 说明 |
|------|---------|------|
| I2S DMA 采集 | 0% | 纯硬件搬运 |
| PDM→PCM | ~3% | CIC 软件抽取 |
| 256 点 FFT | ~0.1% | DSP 指令加速 |
| 32 维 MFCC | ~0.2% | 滤波组+log+DCT |
| 阈值比对 | ≈0 | 一次比较 |
| FreeRTOS + OLED + RS485 | ~5% | 系统开销 |
| **合计** | **< 10%** | F407@168MHz，大量余量 |

| 内存占用 | 大小 | 说明 |
|---------|------|------|
| I2S DMA 双缓冲 | ~2KB | |
| FFT 工作区 | ~2KB | |
| MFCC 中间量 | ~1KB | |
| OLED 帧缓冲 | ~1KB | |
| FreeRTOS + 任务栈 | ~30KB | |
| 特征模版库 | ~8KB | 正常状态的多组特征向量 |
| **合计** | **~44KB** | 192KB SRAM，剩余 148KB |

---

## 3. 系统架构

```
                     RS485 总线 (A/B 两根杜邦线 + 面包板汇接)
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────┴────┐       ┌────┴────┐       ┌─────┴─────┐
   │ MAX3485 │       │ MAX3485 │       │ risym U485│
   │ (3.3V)  │       │ (3.3V)  │       │ (蓝色盒子) │
   └────┬────┘       └────┬────┘       └─────┬─────┘
        │                  │                  │
   ┌────┴────┐       ┌────┴────┐           USB→PC
   │  ESP32  │       │ STM32F4 │        ┌──────────┐
   │ 天气采集│       │ 音频检测│        │PC 上位机  │
   │ 0x01    │       │ 0x02    │        │(主站 0x00)│
   └─────────┘       └─────────┘        │PyQt 频谱  │
        │                  │             │报警+日志  │
        ▼                  ▼             └──────────┘
   气温+湿度+         检测结果+
   天气描述           报警状态
```

### 3.1 单节点内部数据流

```
INMP441 ──I2S──→ DMA 双缓冲 ──→ PDM→PCM ──→ 分帧+汉明窗
                                              │
                                              ▼
                                        256点 FFT
                                              │
                                              ▼
                                    Mel 滤波器组 (32维)
                                              │
                                              ▼
                                    MFCC 特征向量 (取前13维)
                                              │
                                    ┌─────────┴─────────┐
                                    ▼                   ▼
                            频谱显示到OLED        模版库比对
                              (可选)         (欧氏距离/余弦相似度)
                                                    │
                                        ┌───────────┴───────────┐
                                        ▼                       ▼
                                    距离 < 阈值               距离 > 阈值
                                        │                       │
                                        ▼                       ▼
                                    ● 正常                   ● 异常
                                                           ┌───┴───┐
                                                           ▼       ▼
                                                        LED红  蜂鸣器
                                                        │
                                                        ▼
                                                     RS485上报
```

### 3.2 异常判断逻辑

```
检测流程 (每帧, 帧移 ~16ms):

1. 计算当前帧的 MFCC 特征向量 (13维)
2. 与"正常模版库"中的每个参考向量计算欧氏距离
3. 取最小距离 d_min
4. 如果 d_min > 阈值 → 累计异常帧计数
5. 如果连续 N 帧异常 → 触发报警
6. 如果 d_min ≤ 阈值 → 重置异常帧计数

防抖机制:
  - 连续 ≥ 30 帧 (约 0.5s) 异常才报警，过滤瞬时噪声
  - 报警后需连续 ≥ 60 帧 (约 1s) 正常才解除
```

---

## 4. 硬件清单

### 4.1 核心物料

| 序号 | 物料 | 型号 | 数量 | 估价 | 作用 |
|------|------|------|------|------|------|
| 1 | MCU 核心板 | STM32F407ZGT6 | 1 | 已有 | 主控（音频检测） |
| 2 | 天气采集模块 | ESP32 开发板 | 1 | 已有 | 天气数据采集 + RS485 上报 |
| 3 | 数字麦克风 | INMP441 (I2S) | 1 | 8~15元 | 音频采集 |
| 4 | TTL转RS485模块 | **深圳优信 MAX3485** (3.3V) | **2** | 3~5元/个 | STM32端+ESP32端总线驱动 |
| 5 | USB转RS485模块 | **risym U485 蓝色版** (CH340+TVS) | 1 | ~13元 | PC端总线接入 |
| 6 | OLED 显示屏 | 0.96寸 SSD1306 I2C | 1 | 8~12元 | 频谱/状态显示 |
| 7 | 温湿度传感器 | SHT30 I2C | 1 | 5~8元 | 环境监测 |
| 8 | LED 指示灯 | 红/绿 5mm | 各1 | <1元 | 报警/状态指示 |
| 9 | 有源蜂鸣器 | 3.3V | 1 | 2~3元 | 异常声响报警 |
| 10 | 面包板/杜邦线 | — | 若干 | ~10元 | 原型搭建 |

**待购 BOM 总计：约 50~65 元**（MCU 核心板 + ESP32 已有）

### 4.1.1 RS485 模块选购确认

| 端 | 模块 | 型号 | 排针 | 关键点 |
|----|------|------|------|--------|
| STM32 | TTL↔RS485 | 优信 MAX3485 | 要已焊排针版，到手即用 | 下单前问客服确认 |
| ESP32 | TTL↔RS485 | 优信 MAX3485 | 同上 | 同上 |
| PC | USB↔RS485 | risym U485 蓝色版 | 螺丝端子，自带 USB 线 | CH340 + TVS + 浪涌保护 |

### 4.2 引脚分配

#### STM32F4 端

| 外设 | GPIO | AF | 功能 |
|------|------|----|------|
| I2S3_CK | PC10 | AF6 | INMP441 位时钟 |
| I2S3_WS | PA4 | AF6 | INMP441 字选 |
| I2S3_SD | PC12 | AF6 | INMP441 数据输入 |
| I2C1_SCL | PB6 | AF4 | OLED + SHT30 |
| I2C1_SDA | PB7 | AF4 | OLED + SHT30 |
| USART3_TX | PD8 | AF7 | RS485 → MAX3485 DI |
| USART3_RX | PD9 | AF7 | RS485 ← MAX3485 RO |
| RS485_DE | PD11 | GPIO | MAX3485 DE/RE 方向控制 |
| SPI2_SCK | PB10 | AF5 | Flash 存储 |
| SPI2_MISO | PC2 | AF5 | Flash |
| SPI2_MOSI | PC3 | AF5 | Flash |
| Flash_CS | PB12 | GPIO | Flash 片选 |
| LED_ALARM | PB0 | TIM3 CH3 | 报警灯 (红，PWM快闪/慢闪) |
| LED_STATUS | PB1 | TIM3 CH4 | 状态灯 (绿，常亮/灭) |
| BUZZER | PE6 | GPIO | 蜂鸣器 |
| USART1_TX | PA9 | AF7 | 调试串口 |
| USART1_RX | PA10 | AF7 | 调试串口 |

#### ESP32 端 (天气采集)

| 外设 | GPIO | 功能 |
|------|------|------|
| UART2_TX | GPIO17 | RS485 → MAX3485 DI |
| UART2_RX | GPIO16 | RS485 ← MAX3485 RO |
| RS485_DE | GPIO18 | MAX3485 DE/RE 方向控制 |
| ADC1_CH6 | GPIO34 | 电池电压 (原有) |

> 全部引脚无 JTAG/SWD/BOOT/晶振冲突，随时可接调试器。

#### RS485 节点地址分配

| 地址 | 设备 | 角色 | 上报内容 |
|------|------|------|---------|
| 0x00 | PC (risym U485) | 主站 | 轮询各节点 |
| 0x01 | ESP32 | 从站 | 气温、湿度、天气描述 (~60s) |
| 0x02 | STM32F4 | 从站 | 检测结果、报警状态、频谱摘要 (~100ms) |

#### RS485 面包板汇接

```
risym U485         面包板 A排        MAX3485 ×2
  A ──────────→  A排 ───→ ESP32 MAX3485 A
                      └──→ STM32 MAX3485 A
  B ──────────→  B排 ───→ ESP32 MAX3485 B
                      └──→ STM32 MAX3485 B
```

每个螺丝端子只拧一根杜邦线到面包板，面包板同一排自动并联。零焊接、零缠线。

---

## 5. 软件架构

### 5.1 FreeRTOS 任务划分

| 任务 | 优先级 | 栈大小 | 周期 | 职责 |
|------|--------|--------|------|------|
| TaskAudio | High | 4096 | DMA中断驱动 | I2S 双缓冲采集 + PDM→PCM + 分帧 |
| TaskDetect | AboveNormal | 4096 | 每帧触发 (~16ms) | FFT + MFCC + 模版比对 + 阈值判决 |
| TaskRS485 | Normal | 2048 | 事件/轮询 | 帧解析、组包上报、命令响应 |
| TaskDisplay | Normal | 4096 | 200ms | OLED 频谱/状态刷新 |
| TaskMonitor | Low | 2048 | 1s | LED/蜂鸣器控制、看门狗喂狗 |
| TaskEnv | Low | 1024 | 5s | SHT30 温湿度采集 |

### 5.2 Phase A — MFCC 检测流水线（当前实现）

```
原始音频 (16kHz, 16bit, 512 samples = 32ms)
        │
        ▼
    预加重 (α = 0.97)
        │
        ▼
    分帧 (帧长 25ms = 400 samples, 帧移 10ms = 160 samples)
        │
        ▼
    汉明窗
        │
        ▼
    256 点 FFT (ARM CMSIS-DSP: arm_rfft_fast_f32)
        │
        ▼
    Mel 滤波器组 (32 个三角滤波器, 覆盖 300Hz~8kHz)
        │
        ▼
    Log |能量| → DCT-II → 取前 13 维
        │
        ▼
    MFCC 特征向量 (13 维)
        │
        ▼
    与正常模版库比对 (欧氏距离 / 余弦相似度)
        │
        ▼
    d_min < 阈值 → 正常
    d_min > 阈值 + 连续 N 帧 → 异常报警
```

### 5.3 正常模版库建立方法

```
校准流程 (PC 上位机配合):

1. 设备在正常状态运行时，连续采集 60 秒音频
2. 每帧 (10ms步进) 提取一组 13 维 MFCC
3. 对 6000 组特征做 K-means 聚类 (K=16)
4. 取 16 个聚类中心作为"正常模版"
5. 阈值 = 所有模版到各自簇内样本的平均距离 × 3 (3σ)
6. 参数 (模版+阈值) 通过 RS485 下发给节点，存入 Flash
```

### 5.4 RS485 协议帧

```
┌──────┬──────┬──────┬──────┬──────────────┬──────┬──────┐
│ 帧头  │ 地址  │ 类型  │ 长度  │    载荷       │ CRC16│ 帧尾  │
│ 0xA5 │ 1B   │ 1B   │ 1B   │  N bytes     │ 2B   │ 0x5A │
└──────┴──────┴──────┴──────┴──────────────┴──────┴──────┘
```

| 字段 | 值 | 说明 |
|------|-----|------|
| 帧头 | 0xA5 | 固定 |
| 地址 | 0x01~0xFE | 节点地址，0xFF=广播 |
| 类型 | — | 见下表 |
| 长度 | 0~247 | 载荷字节数 |
| CRC16 | — | Modbus CRC (多项式 0x8005) |
| 帧尾 | 0x5A | 固定 |

**帧类型定义：**

| 类型码 | 名称 | 方向 | 载荷 |
|--------|------|------|------|
| 0x01 | 心跳 | 节点→上位机 | `{uptime_sec, cpu_usage, ram_free}` |
| 0x02 | 检测结果 | 节点→上位机 | `{status, distance, threshold, frame_count}` |
| 0x03 | 频谱摘要 | 节点→上位机 | `{32维Mel频带能量}` |
| 0x04 | 模版更新 | 上位机→节点 | `{16组×13维MFCC模版 + 阈值}` |
| 0x05 | 校准开始 | 上位机→节点 | `{duration_sec}` |
| 0x81 | 应答 | 双向 | `{ack_type, result_code}` |

**主从轮询**：上位机定时轮询各节点，节点不主动发起，避免总线冲突。

---

## 6. 开发路线图

### Phase A — 传统 DSP 检测（第 1-3 周）★ 当前阶段

- [ ] **A1: I2S 音频采集链路**
  - INMP441 I2S3 驱动初始化 (PLLI2S, 16kHz, 16bit)
  - DMA 双缓冲 (ping-pong) 无丢帧采集
  - PDM→PCM CIC 抽取滤波器
  - 16kHz 16bit PCM 数据验证（串口打印波形到 PC 端验证）

- [ ] **A2: FFT + 频谱可视化**
  - CMSIS-DSP 库集成 (`arm_rfft_fast_f32`)
  - 256 点 FFT + 幅度谱计算
  - OLED 实时频谱柱状图显示 (32 频带)
  - 调试串口输出频谱数据

- [ ] **A3: MFCC 特征提取**
  - 预加重 + 分帧 + 汉明窗
  - Mel 滤波器组 (32 个三角滤波器)
  - Log 能量 + DCT-II (取前 13 维)
  - 特征向量验证（确保数值范围合理）

- [ ] **A4: 模版比对 + 阈值检测**
  - 离线采集正常音频 → PC 端 K-means 聚类 → 生成模版
  - 欧氏距离 / 余弦相似度实时比对
  - 连续帧防抖逻辑
  - 防抖参数可调（异常帧阈值、恢复帧阈值）

- [ ] **A5: RS485 通信**
  - USART3 + MAX485 驱动 (DE/RE 方向控制)
  - 协议帧打包/解析 (含 CRC16 校验)
  - PC 端 USB转RS485 收发验证
  - 主从轮询时序测试

- [ ] **A6: 报警输出**
  - 红灯 PWM 快闪 (异常) / 慢闪 (警告) / 灭 (正常)
  - 绿灯常亮 (系统正常) / 灭 (故障)
  - 蜂鸣器断续报警

- [ ] **A7: 辅助功能**
  - SHT30 温湿度采集 (I2C1)
  - 看门狗 (IWDG)
  - Flash 存储：模版参数 + 事件日志

### Phase B — 数据积累 + 精度打磨（第 4-5 周）

- [ ] **B1: PC 上位机 MVP**
  - 串口通信 (USB转RS485)
  - 实时频谱瀑布图显示
  - 报警状态面板
  - 一键"开始采集样本"

- [ ] **B2: 样本数据库**
  - 录制多种工况：正常运转、轴承异响、齿轮咬合异常、松动共振
  - 每类 ≥ 100 个片段 (3~5秒/段)
  - 标注 + 切分 + 入库

- [ ] **B3: 阈值精度调优**
  - 用标注数据回测，计算误报率/漏报率
  - 优化模版数量 (K 值) 和阈值倍数
  - 不同设备类型用不同参数集

- [ ] **B4: 稳定性测试**
  - 连续运行 24h，记录误报次数
  - 多节点 (≥2) RS485 组网稳定性
  - 高低温环境 (0~50°C) 传感器漂移测试

### Phase C — CMSIS-NN 模型升级（第 6-8 周）★ 可选

- [ ] **C1: 模型训练 (PC端)**
  - 用 Phase B 的 MFCC 特征数据训练二分类网络
  - TensorFlow Lite: 13→16→8→2 全连接
  - int8 量化

- [ ] **C2: 模型部署**
  - CMSIS-NN 推理引擎集成
  - 替换阈值模块，接口不变
  - 推理延迟 + 精度对比

- [ ] **C3: 优化迭代**
  - 在线自适应阈值校准
  - 模型 OTA 更新机制

---

## 7. 项目结构

```
SoundDog/
├── PROJECT_PLAN.md              ← 本文件
├── README.md
├── firmware/                     # STM32F4 固件
│   ├── Core/
│   │   ├── Inc/
│   │   │   ├── pin_config.h     # 引脚映射
│   │   │   ├── app_config.h     # 应用参数
│   │   │   └── sounddog.h       # 全局数据结构
│   │   └── Src/
│   │       ├── main.c
│   │       ├── audio/           # I2S 驱动 + PDM→PCM
│   │       │   ├── i2s_drv.c
│   │       │   └── pdm_decim.c
│   │       ├── dsp/             # FFT + MFCC
│   │       │   ├── fft.c
│   │       │   ├── mel_filter.c
│   │       │   └── mfcc.c
│   │       ├── detect/          # 模版比对 + 阈值判决
│   │       │   ├── template.c
│   │       │   └── detector.c
│   │       ├── comm/            # RS485 协议栈
│   │       │   ├── rs485_drv.c
│   │       │   └── protocol.c
│   │       ├── display/         # OLED 驱动
│   │       │   └── oled.c
│   │       ├── sensors/         # SHT30 温湿度
│   │       │   └── sht30.c
│   │       └── storage/         # Flash 存储
│   │           └── flash_kv.c
│   └── Drivers/
├── host/                         # PC 上位机
│   ├── main.py                   # PyQt 主程序
│   ├── rs485_proto.py            # 协议解析
│   ├── spectrogram.py            # 频谱瀑布图
│   ├── calibrate.py              # 模版校准工具
│   └── requirements.txt
├── tools/                        # 辅助脚本
│   ├── audio_capture.py          # PC端样本录制
│   └── template_train.py         # K-means 聚类生成模版
├── samples/                      # 音频样本库 (Phase B)
│   ├── normal/
│   └── abnormal/
└── docs/
    ├── protocol.md               # RS485 协议文档
    ├── hardware.md               # 硬件接线图 + BOM
    └── calibration_guide.md      # 校准流程指南
```

---

## 8. 关键指标

### Phase A 指标（传统 DSP）

| 指标 | 目标 | 验证方式 |
|------|------|---------|
| 音频采样率 | 16kHz / 16bit / 单声道 | 串口打印波形 |
| DMA 丢帧率 | 0% | 双缓冲序号连续 |
| FFT 分辨率 | 256 点, ~62.5 Hz/bin | FFT 结果验证 |
| MFCC 维度 | 13 维 | 串口打印特征向量 |
| 检测延迟 | < 50ms (3帧) | 示波器测 GPIO toggle |
| 报警防抖 | 连续 30 帧异常 (≈0.5s) | 参数可调 |
| RS485 波特率 | 115200 (可升至 921600) | 串口助手收发验证 |
| CPU 占用 | < 10% @ 168MHz | FreeRTOS 任务统计 |
| RAM 占用 | < 50KB | 编译后 .bss + .data |

### Phase A 检测精度（定性目标）

| 指标 | 目标 |
|------|------|
| 正常状态识别率 | > 95%（不误报） |
| 明显异响检出率 | > 90%（不遗漏） |
| 轻微异响检出率 | > 70%（尽力） |

---

## 9. 风险与应对

| 风险 | 影响 | 概率 | 应对 |
|------|------|------|------|
| INMP441 PDM 处理复杂，CIC 滤波器写错 | 采集链路不通 | 中 | 先用已知 STM32 PDM 库验证；备选：换 I2S PCM 麦克风 (SPH0645) |
| 面包板 I2S 高频信号跳动 | 数据丢帧/误码 | 中 | 降低采样率到 8kHz 先跑通；引脚飞线尽量短 |
| MFCC 阈值难以确定 | 误报或漏报 | 中 | 做校准工具 + 参数可在线调；引入多档阈值（宽松/标准/严格） |
| 正常模版漂移（设备老化/环境变化） | 长期误报增多 | 低 | 定期重新校准；上位机一键下发新模版 |
| RS485 多节点时序冲突 | 数据碰撞丢帧 | 低 | 严格主从轮询，节点不主动发；可加地址防冲突 |
| OLED 刷新占用 SPI 影响 I2S | 音频断流 | 低 | OLED 走 I2C 独立总线，不冲突 |

---

## 10. 扩展方向

- **无线版**：RS485 → WiFi/4G (ESP32 做通信协处理器)
- **传感器融合**：加加速度计/陀螺仪 (振动+声音联合判断)
- **模型升级**：F407 传统 DSP → F407 CMSIS-NN → H743 → STM32N6 (内置 NPU)
- **云端对接**：网关汇聚 → MQTT → 云平台仪表盘
- **自适应阈值**：运行时在线更新正常模版，适应设备老化漂移
