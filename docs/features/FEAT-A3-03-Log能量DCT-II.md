# FEAT-A3-03-Log能量DCT-II

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行 + 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。
> 事实来源：`.sop-agent规格.md` §3（禁止自造数值）；规格未给出的数值一律写"待定（需人确认）"。

## 0. 元数据
- **父FEAT**：FEAT-A3-MFCC特征提取
- **优先级**：P1
- **预估总耗时**：2h
- **当前状态**：🔴未开始（傻瓜式操作单已备好；依赖 A3-02 通过后开跑）

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）
> 对 A3-02 的 32 维 Mel 能量取 Log 后做 DCT-II（N=32），取前 13 维输出 MFCC 特征向量，供 A3-04 验证与 A4 模版比对使用。

### 1.2 地基触碰前置自检
- [x] 否 → 继续（App 层 DSP 代码，不碰 `firmware/Drivers/` HAL 与 CMSIS-DSP 库源码）

### 1.3 任务边界
| 类型 | 具体内容 |
|------|----------|
| 范围内（可改） | 新增 `firmware/soundDog/Src/dsp/mfcc_dct.c`（Log 能量 + DCT-II；目录以 A2-02/A3-01 现有 dsp 结构为准）、对应头文件、`firmware/soundDog/Makefile`（C_SOURCES 加源） |
| 超出范围（禁止碰） | `firmware/Drivers/` HAL 底层库、CMSIS-DSP 库源码 |
| 外部依赖 | A3-02 的 32 维 Mel 能量、A1-04 采样率核实 |

### 1.4 关键参数（事实包 §3.6 A3-03，禁止自造）
- 输入：A3-02 的 **32 维 Mel 能量**。
- **Log 能量**：对 32 维 Mel 能量取对数（**底数：待定（需人确认）**，常用 log10 或 ln，实现时全流程统一）。
- **DCT-II**（标准定义）：`X_k = Σ_{n=0}^{N-1} x_n · cos(π·k·(2n+1)/(2N))`，N=32。
- 输出：**取前 13 维 MFCC**（事实包：取前 13 维；即 k=0..12）。
- **归一化系数 / 是否正交归一：待定（需人确认）**（不影响维度，影响数值范围，A4 建模前须统一）。
- 采样率 **Fs = 16kHz**（A1-04 核实后为准）。
- 输出维度：**13 维 MFCC 向量**。

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）
- **阶段目标**：确认 A3-02 已通过，定位文件，输出影响范围
- [ ] 步骤1：确认 A3-02 已通过（32 维 Mel 能量输出正常，记录在 A3-02 日志）
- [ ] 步骤2：确认 Log 底数与 DCT 归一化结论（§1.4 待定项是否已定；未定则标记阻塞等确认）
- [ ] 步骤3：读 `dsp/mel.c`（A3-02 输出接口）、`dsp/mfcc.c`（A3-01，若合并实现则扩展该文件）
- [ ] 步骤4：输出影响范围（新增 Log/DCT 模块、接口、风险）
- **判定标准**：影响范围清单列出，Log 底数/归一化已确认或明确标记阻塞
- **⏸️ 停等：人确认后进入阶段 2**

### 🔵 阶段 2：设计（角色：Dev Agent）
- **阶段目标**：设计 Log+DCT 数据流，产出 §3 傻瓜式操作单
- [ ] 步骤1：Log 处理设计（含 0/负值防护：如 log(x+ε)，ε 取值待定（需人确认））
- [ ] 步骤2：DCT-II（N=32）实现方案与 13 维截取接口设计
- [ ] 步骤3：产出 §3 操作单
- **判定标准**：§3 可照做执行；全部参数与 §1.4 一致，无自造数值
- **⏸️ 停等：人确认后进入阶段 3**

### 🟠 阶段 3：实现（角色：Dev Agent）
- **阶段目标**：写代码，每改完一个文件对照 AC 自检
- [ ] 步骤1：新增 `dsp/mfcc_dct.c` + 头文件：Log 能量（底数按阶段1结论）
- [ ] 步骤2：DCT-II（N=32，标准公式）→ 取前 13 维输出
- [ ] 步骤3：`Makefile` C_SOURCES 添加新源文件（BUG-002 教训：漏加导致链接失败）
- [ ] 步骤4：运行 `firmware\build.bat`，编译无错误、无新增警告
- **判定标准**：编译通过（`build.bat` 无错误退出，无新增警告）
- **⏸️ 停等：人确认后进入阶段 4**

===== 切换至 Test Agent =====

### 🟣 阶段 4：测试（角色：Test Agent）
- **阶段目标**：独立测试——只看规格（§1.4/§3/§4），不看 Dev 实现细节
- [ ] 步骤1：核对输出**维度恒为 13**、无 NaN/INF（记录实测）
- [ ] 步骤2：静音 vs 吹气/纯音对比：13 维整体能量低 vs 明显变化（记录实测范围）
- [ ] 步骤3：运行 `./scripts/ci_local.sh`，输出报告
- [ ] 若失败 → 退回阶段 3
- **判定标准**：AC-01/AC-02 有实测证据（记录实际值才算通过）
- **⏸️ 停等：人确认后进入阶段 5**

===== 切换至 Review Agent =====

### 🔴 阶段 5：审查（角色：Review Agent）
- **阶段目标**：独立审查——安全检查、代码质量、规范合规、AC 逐条打勾
- [ ] 步骤1：核对 §4 全部 AC 勾选与记录值
- [ ] 步骤2：核对 13 维 MFCC 输出（数据类型/顺序）与 A4-01 输入约定一致
- **判定标准**：AC 全部通过且记录值完整
- 全部通过 → ✅ 回填「维护与调试」+ 父FEAT项目表 + INDEX + 维护地图
- 未通过 → 退回对应阶段并附意见
- **⏸️ 停等：人确认 Review 通过后归档**

## 3. 傻瓜式操作单（照做即可）

### 3.0 物品/工具清单（动手前先打勾，缺一不可）
- [ ] 开发机（mingw32-make + arm-none-eabi-gcc 环境）
- [ ] 核心板（A1/A2/A3-01/A3-02 链路已验证通过）
- [ ] ST-Link（SWD：SWDIO/SWCLK/GND/3.3V 四线）
- [ ] USB-TTL（CH340）+ SSCOM 串口助手（115200，COM 以设备管理器为准）
- [ ] 5V 电源：电脑 USB 口或非快充 5V 适配器（**供电红线：严禁手机快充头**）

### 3.1 编译（阶段 3 执行）
1. 打开 `firmware` 目录，运行 `build.bat`。
2. 期望：编译**无错误退出**，**无新增警告**（记录警告数：___）。
3. 失败 → 查 §3.4 故障速查表。

### 3.2 烧录（阶段 3/4 执行；插拔 ST-Link 前先断电源）
1. 接好 ST-Link 四线，运行 `firmware\build_and_flash.bat`。
2. 期望：OpenOCD 输出 `wrote ... bytes` / `Verified OK`（记录：___）。
3. **失败兜底**：OpenOCD 连不上 → 查四线/驱动；仍不行 → `firmware\flash_isp.bat`（BOOT0+USART1，HANDOFF §4.3）。

### 3.3 串口核对（阶段 4 执行）
1. USB-TTL 接核心板：TX↔RX **交叉**、GND 共地；SSCOM 115200。
2. 按复位键，期望依次出现三行：`SoundDog boot OK` → `I2S_DRV_Init ret=0` → `I2S DMA started`。
3. 观察 MFCC 打印（打印格式：**待定（需人确认）**）：期望**维度 = 13**（记录：___ 维），无 NaN/INF。
4. 静音 5 秒：记录 13 维（记录数值：___），期望整体能量低。
5. 播放 1kHz 正弦（A2-02 同款声源）5 秒：记录 13 维（记录数值：___），期望与静音明显不同。
6. 吹气 5 秒：记录 13 维（记录数值：___），期望与静音/纯音均有明显差异。

### 3.4 故障速查表
| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 输出含 NaN / INF | Log 输入为 0 或负值未防护 | 加 log(x+ε) 防护，ε 待定（需人确认） |
| 输出维度 ≠13 | 前 13 维截取实现错 | 核对 DCT 输出 k=0..12 截取 |
| 数值恒定不变 | 输入 Mel 能量恒定（链路断裂） | 回溯 A3-02 输出是否随声音变化 |
| 全 0 | 输入全 0 | 回溯 A3-02/A3-01 各段打印 |
| 编译链接失败 | Makefile 漏加源（BUG-002 教训） | C_SOURCES 加 `dsp/mfcc_dct.c` 重编 |

## 4. 验收标准（12 条）

### 功能验收
- [ ] AC-01 Log 能量 + DCT-II 实现，输出恒为**前 13 维 MFCC**（维度=13 可测量，记录于 §3.3）
- [ ] AC-02 输出无 NaN/INF；静音与有声（纯音/吹气）13 维数值差异明显（记录实测范围）

### 代码质量
- [ ] AC-03 编译通过（`build.bat` 无错误退出）
- [ ] AC-04 无新增警告（记录警告数）
- [ ] AC-05 文档已更新（本 FEAT §5 维护章节 + 父FEAT项目表文件列回填）

### 安全红线
- [ ] AC-06 无硬编码密钥 —— **N/A**（嵌入式 DSP 代码，无密钥/凭据概念）
- [ ] AC-07 错误处理完善：Log 输入 0/负值防护、数组越界防护（边界条件下不崩溃、不产生 NaN）
- [ ] AC-08 用户输入已校验 —— **N/A**（无外部用户输入；输入为内部 Mel 能量）

### 测试与边界
- [ ] AC-09 新增测试自动运行 —— **N/A**（项目无自动化测试框架，验证走 §3 串口核对）
- [ ] AC-10 回归：A1-02 / A2-02 / A3-01 / A3-02 输出不被破坏
- [ ] AC-11 测试覆盖率 ≥80% —— **N/A**（无覆盖率工具）
- [ ] AC-12 按 FEAT 模板执行：5 阶段按序、角色声明、⏸️ 停等、日志完整

## 5. 维护与调试（阶段5通过后回填）

### 5.1 影响文件
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `firmware/soundDog/Src/dsp/mfcc_dct.c` | 新增 | Log 能量 + DCT-II + 13 维截取 |
| `firmware/soundDog/Inc/dsp/mfcc_dct.h`（路径以现有 dsp 结构为准） | 新增 | MFCC 接口声明 |
| `firmware/soundDog/Makefile` | 修改 | C_SOURCES 加 `dsp/mfcc_dct.c` |

### 5.2 调试要点
- NaN/INF → Log 0/负值防护；维度错 → 13 维截取核对
- 数值恒定 → 回溯 A3-02 Mel 能量

### 5.3 测试入口
- `firmware\build.bat` + `firmware\build_and_flash.bat` + SSCOM 115200（对应 AC-01/AC-02）

## 6. 执行日志

| 时间 | 阶段 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| | | | | |
