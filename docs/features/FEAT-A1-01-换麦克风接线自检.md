# FEAT-A1-01-换麦克风接线自检

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行 + 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。

## 0. 元数据
- **父FEAT**：FEAT-A1-音频采集链路
- **优先级**：P0
- **预估总耗时**：0.5h
- **当前状态**：🟡阶段2进行中（傻瓜式操作单已产出，等待确认进入阶段3）

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）
> 新 INMP441 麦克风到手后，按 §3 操作单**照做即可**：断电接线 → 上电前量电压 → 上电观察 → 串口确认，全程无需现场判断。

### 1.2 地基触碰前置自检
- [x] 否 → 继续（纯硬件接线，不碰代码/底层库）

### 1.3 任务边界
| 类型 | 具体内容 |
|------|----------|
| 范围内（可改） | 麦克风接线、上电自检、电压测量 |
| 超出范围（禁止碰） | `firmware/Drivers/` HAL 底层库、改代码 |
| 外部依赖 | 新 INMP441 麦克风、杜邦线、万用表、5V 电源 |

### 1.4 接线规范（权威：HANDOFF §4.2 + `pin_config.h`）
- **供电红线**：只准电脑 USB 口或非快充 5V 适配器供电，**严禁手机快充头**（上次超压烧板事故根因）。
- 引脚表：VDD→3.3V、GND→GND、L/R→GND（必接）、SCK→PB3、WS→PA4、SD→PB5（+4.7kΩ 上拉至 3.3V）。
- 辅助外设（本 FEAT 可不接）：红灯→220Ω→PB0、绿灯→220Ω→PB1、蜂鸣器→三极管→PE6。

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）✅ 已完成
- [x] 定位文件：`pin_config.h`（引脚权威）、`HANDOFF.md` §4.2（接线规范）
- [x] 影响范围：无代码改动；风险 = 供电红线 / L/R 悬空 / SD 上拉 / 线序核对 / 模块 VDD 电压
- [x] 确认外部依赖：新 INMP441 未到货（当前唯一阻塞）

### 🔵 阶段 2：设计（角色：Dev Agent）🟡 进行中
- **阶段目标**：产出傻瓜式操作单（§3），把"怎么做"写成不用动脑的步骤
- [x] 步骤1：工具/物品清单（§3.0）
- [x] 步骤2：断电接线步骤表，逐根核对（§3.1）
- [x] 步骤3：上电前电压测量步骤与合格范围（§3.2）
- [x] 步骤4：上电观察与异常处理（§3.3）
- [x] 步骤5：故障速查表（§3.5）
- **⏸️ 停等：人确认操作单后进入阶段 3**

### 🟠 阶段 3：实现（角色：Dev Agent）
- **阶段目标**：执行 §3.1~§3.3（接线 → 测量 → 上电），逐行打勾
- [ ] 步骤1：按 §3.1 断电接线，每根打勾
- [ ] 步骤2：按 §3.2 上电前量电压，记录数值
- [ ] 步骤3：按 §3.3 上电观察温度
- **⏸️ 停等：人确认后进入阶段 4**

### 🟣 阶段 4：测试（角色：Test Agent）
- **阶段目标**：独立验证——只按 §3 操作单与 §4 验收标准复核，不看阶段 3 的实现过程
- [ ] 步骤1：按 §3.4 串口确认（boot 三行 + 吹气 max 变化）
- [ ] 步骤2：逐条核对 §4 验收标准
- [ ] 若失败 → 查 §3.5 故障速查表，退回阶段 3
- **⏸️ 停等：人确认后进入阶段 5**

### 🔴 阶段 5：审查（角色：Review Agent）
- **阶段目标**：独立审查——安全红线（供电）、AC 逐条打勾
- 全部通过 → ✅ 回填「维护与调试」+ 父FEAT项目表 + INDEX + 维护地图
- 未通过 → 退回对应阶段并附意见

## 3. 傻瓜式操作单（照做即可）

### 3.0 物品清单（动手前先打勾，缺一不可）
- [ ] INMP441 模块 ×1（原装芯片、已焊排针）
- [ ] 杜邦线（母对母）×6
- [ ] 4.7kΩ 电阻 ×1（SD 上拉，建议加）
- [ ] 万用表（直流电压档）
- [ ] 5V 电源：电脑 USB 口 **或** 非快充 5V 适配器（**严禁手机快充头**）
- [ ] USB-TTL（CH340）+ SSCOM 串口助手（阶段 4 用）

### 3.1 接线（全程断电！先拔掉所有电源线再动线）
按顺序接线，**每接完一根在 ☐ 打勾**，接错一根就返工：

| 顺序 | 从（INMP441） | 到（核心板） | 核对要点 | 完成 |
|------|-------------|-------------|---------|------|
| 1 | VDD | 3.3V | ⚠️ 千万别接 5V！ | ☐ |
| 2 | GND | GND | — | ☐ |
| 3 | L/R | GND | ⚠️ 必接！悬空 = 无声（之前故障根因之一） | ☐ |
| 4 | SCK | PB3 | — | ☐ |
| 5 | WS | PA4 | — | ☐ |
| 6 | SD | PB5 | 加 4.7kΩ 上拉至 3.3V | ☐ |

### 3.2 上电前测量（红线：**先量再插电**）
1. 万用表拨到**直流电压 20V 档**，红笔接被测脚、黑笔接 GND。
2. 量核心板 **5V** 引脚 → 期望 **4.8 ~ 5.2V**（记录数值：______）
3. 量核心板 **3.3V** 引脚 → 期望 **3.2 ~ 3.4V**（记录数值：______）
4. ⚠️ 任一不在范围 → **断电，换电源，禁止继续**（上次 5V=3.1V 导致启动异常/复位循环）。
5. （断电状态下）万用表拨**电阻档**，量 INMP441 的 VDD 对 GND → 应 **>100Ω**；接近 0Ω → 接线短路，回 §3.1 查线。

### 3.3 上电观察
1. 插上电源。
2. 5 秒内手摸 INMP441 表面：**微温 = 正常**；**发烫 → 立即断电**，大概率 VDD 接到 5V 或短路，回 §3.1 查线。

### 3.4 串口确认（阶段 4 执行；本次若只做接线可跳过，留到 A1-02）
1. USB-TTL 接核心板：TX↔RX **交叉**、GND 共地。
2. SSCOM 打开**设备管理器里看到的 COM 口**（不一定是 COM4），波特率 **115200**。
3. 按复位键，期望依次出现：`SoundDog boot OK` → `I2S_DRV_Init ret=0` → `I2S DMA started`。
4. 对麦克风吹气：`[n] max=xxxx` 数值应**明显变化**（如几百 ↔ 几万）。

### 3.5 故障速查表（出问题先查这里）
| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 量 5V 只有 ~3V | 快充头 / 电源带不动 | 换非快充 5V 适配器或电脑 USB 口 |
| 模块发烫 | VDD 接错 5V / 短路 | 立即断电，重查 §3.1 表 |
| 串口完全无输出 | TX/RX 没交叉 / COM 口选错 / GND 未共地 | 交叉接、看设备管理器、共地 |
| 输出乱码 | 波特率不对 | 改为 115200 |
| 只出 boot 前几行就停 | 供电不稳 | 量 5V/3.3V，换电源 |
| 数据恒 `0000FFFF` | L/R 悬空 / SD 高阻 / 模块损坏 | 确认 L/R→GND；SD 加 4.7kΩ 上拉；仍如此 → 模块坏，联系换货 |
| 吹气数值不变 | 麦克风损坏 / 焊点虚 | 轻敲 PCB 听变化；无反应 → 换模块 |

## 4. 验收标准（12 条）

### 功能验收
- [ ] AC-01 上电后量得 5V∈[4.8, 5.2]、3.3V∈[3.2, 3.4]，无短路发热（手摸微温）
- [ ] AC-02 6 根线全部按 §3.1 表接好并逐根核对（L/R→GND 必接、SD 带 4.7kΩ 上拉）
- [ ] AC-03 串口出现 boot 三行（`boot OK` / `I2S_DRV_Init ret=0` / `I2S DMA started`）
- [ ] AC-04 吹气时 `max` 数值明显变化
- [ ] AC-05 向后兼容：不涉及代码，无接口变化

### 代码质量 / 安全红线 / 测试边界
- [ ] AC-06 ~ AC-12：纯硬件步骤，不适用项标注 **N/A**（无代码 → 无编译/警告/测试；供电红线已列入 §3.2 强制步骤）

## 5. 维护与调试（阶段5通过后回填）

### 5.1 影响文件
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| （无代码改动） | — | 纯硬件接线自检 |

### 5.2 调试要点
- 万用表量核心板 5V/3.3V 引脚电压（§3.2 记录值）
- 电压异常立即断电，勿继续

### 5.3 测试入口
- 人工目视 + 万用表测量 + SSCOM 串口（对应 AC-01 ~ AC-04）

## 6. 执行日志

| 时间 | 阶段 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| 2026-08-16 | 阶段1 准备 | Dev Agent | 接口定位（pin_config.h/HANDOFF §4.2）+ 影响范围：无代码改动，风险=供电红线/L-R/SD上拉/线序核对/模块VDD | 外部依赖：新 INMP441 未到货 |
| 2026-08-16 | 阶段2 设计 | Dev Agent | 产出傻瓜式操作单：物品清单/断电接线表/上电前测量步骤/故障速查表 | 无 |
