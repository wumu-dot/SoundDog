# FEAT-A8-04-参数下发与报警监控

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行 + 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。
> 事实来源：`.sop-agent规格.md` §3 + 父 FEAT-A8（禁止自造数值；未覆盖项写"待定（需人确认）"）。

## 0. 元数据
- **父FEAT**：FEAT-A8-上位机
- **优先级**：P1
- **预估总耗时**：3h
- **当前状态**：🔴未开始（傻瓜式操作单已备好，依赖 A8-01 通信层 + A4-04 参数协议通过后开跑）

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）
> 上位机界面**下发**检测参数（阈值/帧数，对接 A4-04 的 RS485 下发）并**接收监控**板端报警事件（A6 异常标志），实现 **参数可视化 + 报警弹窗 + 日志记录**。

### 1.2 地基触碰前置自检
- [x] 否 → 继续（PC 端 GUI + 帧收发，不碰 `firmware/Drivers/`）

### 1.3 任务边界
| 类型 | 具体内容 |
|------|----------|
| 范围内（可改） | 上位机 `gui/` 参数面板 + 报警处理模块（弹窗/日志） |
| 超出范围（禁止碰） | `firmware/Drivers/` HAL；板端 A4-04/A6 实现（本 FEAT 只做 PC 端下发与接收） |
| 外部依赖 | A8-01 通信层、A4-04 参数协议（帧格式待定需人确认）、A6 报警事件输出、**PySide6（需补装）** |

### 1.4 关键参数与接口（事实包 §3 + 父 FEAT-A8，禁止自造）
- **下发参数**：检测阈值、帧数（A4-04 约定；基线 30/60 帧，事实包 A4-03；**具体参数 ID/范围待定需人确认**）。
- **协议**：经 A8-01 comm 按 A5-02 帧协议下发（帧格式待定需人确认）。
- **报警事件**：板端 A6 输出（异常标志/事件帧，格式**待定需人确认**）。
- **UI**：参数输入面板 + 报警弹窗（消息框）+ 日志文件（路径待定需人确认）。
- **运行环境**：**Anaconda Python 3.13.5**（`C:\Users\wumu2\anaconda3\python.exe`）；需补装 **PySide6**（`-m pip install PySide6`）；⚠️ 勿用 conda 命令。
- **待定（需人确认）**：参数 ID 映射、范围校验规则、报警事件帧格式、日志文件路径/滚动策略。

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）
- **读什么**：`A8-01` 文档（comm 接口）、`A4-04` 文档（参数下发协议）、`A6` 文档（报警输出）、父 `FEAT-A8`
- [ ] 步骤1：确认 A8-01 已通过（comm 可收发）
- [ ] 步骤2：确认 A4-04 参数协议定稿（参数 ID/帧格式，待定需人确认）
- [ ] 步骤3：确认 A6 报警事件输出格式（待定需人确认）
- **判定标准**：依赖状态已核实、协议/格式已明确（或标注待定）
- **⏸️ 停等：人确认后进入阶段 2**

### 🔵 阶段 2：设计（角色：Dev Agent）
- **阶段目标**：设计参数面板与报警处理，产出傻瓜式操作单（§3）
- [ ] 步骤1：设计参数面板（输入控件 + 范围校验 + 下发按钮）
- [ ] 步骤2：设计报警处理（事件帧解析 → 弹窗 + 日志写入）
- [ ] 步骤3：产出 §3 操作单（联调 + 故障表）
- **判定标准**：UI 布局已产出、事件流已设计、待定项已列全
- **⏸️ 停等：人确认后进入阶段 3**

### 🟠 阶段 3：实现（角色：Dev Agent）
- **阶段目标**：写 GUI 参数/报警模块，本地自测
- [ ] 步骤1：实现参数面板（输入 + 校验 + 经 comm 下发）
- [ ] 步骤2：实现报警监听（解析事件帧 → 弹窗 + 日志）
- [ ] 步骤3：`python -m py_compile` + 本地伪造帧自测（AC-03）
- **判定标准**：语法通过、本地自测弹窗/日志生效
- **⏸️ 停等：人确认后进入阶段 4**

===== 切换至 Test Agent =====

### 🟣 阶段 4：测试（角色：Test Agent）
- **阶段目标**：独立验证——按 §3 操作单联调实测，核对 §4 验收标准
- [ ] 步骤1：按 §3.2 参数下发（界面输入 → 板端收到并确认）（AC-01）
- [ ] 步骤2：按 §3.3 报警监控（触发板端报警 → PC 弹窗 + 日志）（AC-02）
- [ ] 若失败 → 查 §3.4 故障表，退回阶段 3
- **判定标准**：AC-01/02 有实际记录数值
- **⏸️ 停等：人确认后进入阶段 5**

===== 切换至 Review Agent =====

### 🔴 阶段 5：审查（角色：Review Agent）
- **阶段目标**：独立审查——AC 逐条打勾、协议与 A4-04/A6 一致
- [ ] 步骤1：核对 §4 全部 AC 勾选与记录值（下发回执、报警弹窗/日志）
- [ ] 步骤2：核对下发帧与 A4-04 协议一致、报警帧解析与 A6 输出一致
- **判定标准**：AC 全部通过且记录值完整
- 全部通过 → ✅ 回填「维护与调试」+ 父FEAT项目表 + INDEX + 维护地图
- 未通过 → 退回对应阶段并附意见
- **⏸️ 停等：人确认 Review 通过后归档**

## 3. 傻瓜式操作单（照做即可）

### 3.0 物品/工具清单（动手前先打勾，缺一不可）
- [ ] PC（Windows）+ **Anaconda Python 3.13.5**（`C:\Users\wumu2\anaconda3\python.exe`，事实包 §3.7）
- [ ] PySide6 已安装（`& C:\Users\wumu2\anaconda3\python.exe -m pip install PySide6`；`-m pip show PySide6` 确认）
- [ ] A8-01 通信层已通过
- [ ] 核心板（A4-04/A6 已在跑，报警可触发）
- [ ] 5V 电源（非快充）+ USB-TTL

### 3.1 环境确认（阶段 1 执行）
1. `& C:\Users\wumu2\anaconda3\python.exe -m pip show PySide6` → 记录版本：___（无 → 先 `-m pip install PySide6`）
2. 确认 A4-04 参数协议（参数 ID/范围/帧格式，记录：___，待定需人确认）

### 3.2 参数下发测试（阶段 4 执行）
1. 启动上位机，连接 CH340（115200）。
2. 参数面板输入新阈值/帧数（如阈值=待定，帧数=30）→ 点击"下发"。
3. 期望：板端收到并回执确认（A4-04 定义的回执帧）。记录：下发成功：是/否；板端回执：___
4. **越界输入测试**：输入超范围值 → 期望界面拦截、不发送。记录：拦截：是/否

### 3.3 报警监控测试（阶段 4 执行）
1. 触发板端报警（制造异常工况，如持续异常声音/按 A6 调试入口）。
2. 期望：PC 端**弹窗报警** + **日志文件追加记录**（时间/类型/数值）。记录：弹窗：是/否；日志行：___
3. 报警解除 → 期望：状态恢复显示。记录：恢复：是/否

### 3.4 故障速查表
| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 下发无回执 | 帧格式与 A4-04 不符 / COM 没连上 | 对照 A4-04 帧定义核对；检查连接 |
| 越界值被发送 | 校验逻辑缺失 | 确认面板有范围校验（规则待定需人确认） |
| 报警不弹窗 | 事件帧解析不符 / A6 未触发 | 对照 A6 输出格式；SSCOM 确认板端确实发出事件帧 |
| 日志没写入 | 日志路径不可写 | 检查路径（待定需人确认）；确认写日志代码被执行 |
| 误报频繁 | 阈值/帧数设置过松 | 调参重发（用本 FEAT 参数面板） |

## 4. 验收标准（12 条）

### 功能验收
- [ ] AC-01 参数下发：板端收到并回执，越界输入被拦截（§3.2 记录）
- [ ] AC-02 报警监控：触发后 PC 弹窗 + 日志追加，解除后恢复（§3.3 记录）
- [ ] AC-03 向后兼容：PC 端独立模块，不影响板端固件

### 代码质量
- [ ] AC-04 语法检查通过（`python -m py_compile`）
- [ ] AC-05 校验与错误处理：越界拦截、无回执/无事件帧有提示
- [ ] AC-06 文档已更新（本 FEAT + 父 FEAT-A8 回填）

### 安全红线
- [ ] AC-07 N/A：无密钥
- [ ] AC-08 输入校验：参数范围有校验（越界不发送）

### 测试与边界
- [ ] AC-09 越界输入用例已执行（§3.2 步骤4）
- [ ] AC-10 N/A：无自动化测试基建（待定需人确认）
- [ ] AC-11 N/A：手动验证，覆盖率不适用
- [ ] AC-12 按 FEAT 模板执行（本文件即模板产物）

## 5. 维护与调试（阶段5通过后回填）

### 5.1 影响文件
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `host_app/gui/params_panel.py` | 新增 | 参数输入/校验/下发 |
| `host_app/gui/alarm_handler.py` | 新增 | 报警弹窗 + 日志 |
| `host_app/core/alarm_log.py` | 新增 | 日志文件写入 |
| `host_app/requirements.txt` | 修改 | 追加 PySide6 依赖 |

### 5.2 调试要点
- 下发/报警帧逐字节打印，对照 A4-04/A6 定义核对
- 日志文件路径与滚动策略记录在配置（待定需人确认）

### 5.3 测试入口
- §3.2 参数下发 + §3.3 报警监控（对应 AC-01/02）

## 6. 执行日志

| 时间 | 阶段 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| | | | | |
