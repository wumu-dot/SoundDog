# FEAT-A1-音频采集链路（父FEAT · 统筹规划）

> **AI 执行规则**：父FEAT 只做统筹规划，禁止编写实现代码；实现一律拆成子FEAT。
> 创建/修改项目表后必须停等，输出结果等人类确认方向后才允许创建子FEAT。
> 禁止自审自判完成：父FEAT 置 🟢 仅当项目表全部子FEAT 🟢 且每个都经过 Review。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。

## 0. 元数据
- **角色**：Planner（统筹规划）
- **优先级**：P1
- **预估总耗时**：4h（含等硬件）
- **当前状态**：🟡部分子FEAT完成（固件已就绪，4 个验证子FEAT 待换麦克风后开）

## 1. 总体目标与方向

### 1.1 核心目标（一句话）
> 打通并验证 INMP441 → I2S3 → DMA 音频采集链路，拿到随声音变化的 PCM 数据，支撑 A2 FFT。

### 1.2 大方向策略
> 固件已就绪（I2S3+DMA 采集已实现），当前只剩换麦克风后硬件验证；先上电自检，再烧录验证链路，最后校准 SCK 时钟。

### 1.3 硬边界（继承 CLAUDE.md）
- [x] 是否涉及底层库 / 第三方库？→ 否（仅 App 层 + 接线，不碰 `firmware/Drivers/`）
- [x] 否 → 继续

## 2. 项目表（子FEAT清单）

> 每个子FEAT 按子模板（`.template-child.md`）创建，独立执行 5 阶段（准备→设计→实现→测试→审查）。

| 子FEAT编号 | 功能 | 验收要点 | 优先级 | 依赖 | 状态 | 文件 |
|-----------|------|----------|--------|------|------|------|
| FEAT-A1-01 | 换麦克风接线与上电自检 | 供电 5V/3.3V 正常，无短路，INMP441 接线正确 | P0 | 无 | 🟡阶段5有条件通过（AC-04 移交 A1-02 已终验 ✅；遗留补录 C3 电压数值/C4 轻敲 PCB） | `FEAT-A1-01-换麦克风接线自检.md` |
| FEAT-A1-02 | 烧录与音频链路验证 | 串口 `[n] max=xxxx` 数值随声音变化 | P0 | 依赖 01 | 🟢已完成（2026-08-28 审查通过：安静 max 160~1606 / 吹气 32767 多次打满，对比~47倍） | `FEAT-A1-02-音频链路验证.md` |
| FEAT-A1-03 | L/R 通道确认 | 明确有效数据所在声道，取数稳定非全 0 | P1 | 依赖 02 | 🟢已完成（2026-08-28：左声道有效，档位 A/B/悬空三组实验互证，档位 B max 恒 1 = 悬空字签名实锤移位预测；无代码改动） | `FEAT-A1-03-LR通道确认.md` |
| FEAT-A1-04 | SCK 时钟核实 | 逻辑分析仪实测 SCK=1.024MHz（非 2.048MHz） | P1 | 依赖 02 | 🟢已完成（2026-08-28：SCK 实测 1MHz±4% 分辨率/WS 实测 16kHz 精确命中/比值 64 自洽，Fs=16000Hz 无误，"2 倍疑点"证伪关闭；无代码改动） | `FEAT-A1-04-SCK时钟核实.md` |

> 子FEAT 完成后回填本行状态与文件链接（🟢已完成 / 🟡阶段X进行中）。

## 3. 依赖与执行顺序

1. A1-01 上电自检 → A1-02 链路验证（硬件不安全则后续无意义）
2. A1-03 通道确认 与 A1-04 时钟核实 可并行（均依赖 02 跑通）

## 4. 阻塞与下一步

- ~~**阻塞**：INMP441 麦克风损坏（SD 高阻无输出），需换新麦克风~~（已解除 2026-08-28：新麦克风到货并接线验证通过）
- **下一步（2026-08-28 更新）**：**A1 全线收官**（01🟡有条件通过/02🟢/03🟢/04🟢），进入 **A2 FFT 频谱分析**（A2-01 数据缓冲/队列 → A2-02 FFT 幅度谱）；A1-01 遗留 C3/C4 补录为可选项不阻塞
- ~~原下一步：A1-03 / A1-04 可并行开~~（均已完成）

## 5. 完成定义（DoD）

- [x] 项目表全部子FEAT 🟢 且每个经 Review（A1-01 为🟡有条件通过：AC-04 已由 A1-02 终验兜底，C3/C4 为可选补录项）
- [x] 本表状态与 `docs/features/INDEX.md` 一致（2026-08-28 核对）
- [x] 经验教训已提炼（BUG-002 详解文档 6+1 条教训，lessons_summary 索引，R24/R25/R26 规则化）

## 6. 执行日志

| 时间 | 动作 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| 2026-08-28 | A1-02 阶段5审查通过 | Review Agent | AC-01/02 ✅（安静 max 160~1606 / 吹气 32767，对比~47倍）；父表 A1-01/A1-02 行回填；阻塞解除；A1-03/04 依据 BUG-002 dump 工作量缩减 | 无 |
| 2026-08-28 | A1-03 阶段5审查通过 | Review Agent | 左声道有效（L/R→GND）；档位 B max 恒 1 = 悬空字签名，实锤 4 字周期移位预测；悬空对照排除设备嫌疑；无代码改动。父表/INDEX 回填 | 杜邦线未插实一次（复插解决，已记录） |
| 2026-08-28 | A1-04 阶段5审查通过 | Review Agent | Saleae Logic 1.2.17 实测（24MHz/1s）：SCK=1MHz±4% 分辨率（真值含 1.024MHz）、WS=16kHz 精确命中、比值 64 自洽 → Fs=16000Hz 无误，"2 倍疑点"证伪关闭。**A1 全线收官，DoD 全勾** | 无 |
