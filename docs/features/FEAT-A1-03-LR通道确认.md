# FEAT-A1-03-LR通道确认

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行 + 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。
> 事实来源：`.sop-agent规格.md` §3（禁止自造数值）。

## 0. 元数据
- **父FEAT**：FEAT-A1-音频采集链路
- **优先级**：P1
- **预估总耗时**：0.5h
- **当前状态**：🔴未开始（傻瓜式操作单已备好，依赖 A1-02 通过后开跑）

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）
> 确认有效 PCM 数据落在左声道还是右声道，固化取数掩码，为 A2 FFT 提供干净数据源。

### 1.2 地基触碰前置自检
- [x] 否 → 继续（不改底层库，仅确认取数逻辑）

### 1.3 任务边界
| 类型 | 具体内容 |
|------|----------|
| 范围内（可改） | `i2s_drv.c` 取数逻辑/打印（如需改声道索引） |
| 超出范围（禁止碰） | `firmware/Drivers/` HAL 底层库 |
| 外部依赖 | 已跑通的音频链路（A1-02）、SSCOM |

### 1.4 关键参数（事实包 §3）
- L/R→GND = **左声道**（INMP441 手册约定）；L/R→3.3V = 右声道。
- 数据在 32 位字**低 16 位**（BUG-002），取数 `(int16_t)(raw & 0xFFFF)`。
- 串口打印 `[n] max=xxxx`，max 随吹气变化 = 该声道有数据。

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）
- **阶段目标**：确认 A1-02 已通过，记录当前取数实现
- [ ] 步骤1：确认 A1-02 已通过（串口 boot 三行 + max 随声音变化）
- [ ] 步骤2：读 `i2s_drv.c` / `i2s_drv.h` 当前取数实现（掩码现状）
- **⏸️ 停等：人确认后进入阶段 2**

### 🔵 阶段 2：设计（角色：Dev Agent）
- **阶段目标**：确定声道判定实验步骤（§3 操作单）
- [ ] 步骤1：设计 L/R→GND 与 L/R→3.3V 两档对比实验
- [ ] 步骤2：确定结论记录格式（声道 + 掩码 + 是否需要改代码）
- **⏸️ 停等：人确认后进入阶段 3**

### 🟠 阶段 3：实现（角色：Dev Agent）
- **阶段目标**：按 §3 执行两档实验，记录数据
- [ ] 步骤1：档位A（L/R→GND，当前接线）：SSCOM 观察吹气 max 变化
- [ ] 步骤2：档位B（断电改 L/R→3.3V）：上电再观察
- [ ] 步骤3：如需改取数逻辑 → 改 `i2s_drv.c` 后 `build_and_flash.bat` 重烧
- **⏸️ 停等：人确认后进入阶段 4**

### 🟣 阶段 4：测试（角色：Test Agent）
- **阶段目标**：独立验证——按 §3 复跑，核对 §4 验收标准
- [ ] 步骤1：复跑有效档位，max 稳定非全 0（AC-01）
- [ ] 步骤2：结论写入本 FEAT 维护章节（AC-01 记录）
- [ ] 若失败 → 退回阶段 3
- **⏸️ 停等：人确认后进入阶段 5**

### 🔴 阶段 5：审查（角色：Review Agent）
- **阶段目标**：独立审查——AC 逐条打勾
- 全部通过 → ✅ 回填「维护与调试」+ 父FEAT项目表 + INDEX + 维护地图
- 未通过 → 退回对应阶段并附意见

## 3. 傻瓜式操作单（照做即可）

### 3.0 物品/工具清单
- [ ] 核心板（已按 A1-01 接线、A1-02 验证通过）
- [ ] SSCOM（115200，COM 以设备管理器为准）
- [ ] 杜邦线 ×1（临时改 L/R 接线用）

### 3.1 档位A：L/R→GND（左声道，当前接线）
1. 上电，SSCOM 打开，确认串口出现 boot 三行 + `[n] max=xxxx`。
2. 对麦克风吹气 3 秒，观察 max：**记录数值范围：___ ~ ___**
3. 若 max 随吹气明显变化 → 左声道有数据；若恒 0 或恒定 → 查 §3.4。

### 3.2 档位B：L/R→3.3V（右声道）
1. **断电**，把 L/R 从 GND 改接到 3.3V（杜邦线）。
2. 上电，SSCOM 观察，吹气 3 秒：**记录数值范围：___ ~ ___**
3. 对比两档：哪档 max 随声音变化 = 有效声道。

### 3.3 结论固化（记录到本 FEAT 维护章节）
- 有效声道：**左 / 右**（圈选）
- 取数掩码：`(int16_t)(raw & 0xFFFF)`（BUG-002 已确认，数据在低 16 位）
- 是否需要改代码：**是 / 否**（如两档都无数据或都有效 → 需改，退回阶段 3 说明）

### 3.4 故障速查表
| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 两档 max 都恒 0 | 取数掩码错误 / SD 无输出 | 查 BUG-002；确认 SD 上拉；确认 A1-02 实际通过 |
| 两档 max 都随声音变 | 未真正切换 L/R（接线没换成功） | 断电重接，确认杜邦线接触 |
| 档位B 完全无数据 | 右声道本来无有效信号（正常现象） | 以档位A 为准，结论=左声道 |
| 吹气无反应 | 麦克风损坏 | 换模块（A1-01 §3.5） |

## 4. 验收标准（12 条）

### 功能验收
- [ ] AC-01 明确记录有效声道 + 取数掩码，串口打印该声道数据稳定且非全 0
- [ ] AC-02 向后兼容（不破坏 A1-02 已通过链路）

### 代码质量 / 安全红线 / 测试边界
- [ ] AC-03 若改代码：编译通过（`build.bat` 无错误）
- [ ] AC-04 若改代码：无新增警告
- [ ] AC-05 结论已写入本 FEAT「维护与调试」章节
- [ ] AC-06 ~ AC-12：不适用项标 **N/A**（纯验证步骤：无密钥/输入/新测试/覆盖率）

## 5. 维护与调试（阶段5通过后回填）

### 5.1 影响文件
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `firmware/soundDog/Src/i2s_drv.c` | 修改（如需） | 声道/掩码取数 |
| `firmware/soundDog/Inc/i2s_drv.h` | 修改（如需） | 取数宏 `I2S_DRV_ExtractPCM` |

### 5.2 调试要点
- 全 0 → BUG-20260816-002 槽偏移；L/R 悬空 → 接地

### 5.3 测试入口
- SSCOM 观察两档 max 对比（对应 AC-01）

## 6. 执行日志

| 时间 | 阶段 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| | | | | |
