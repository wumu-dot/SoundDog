# FEAT-A3-02-Mel滤波器组

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行 + 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。
> 事实来源：`.sop-agent规格.md` §3（禁止自造数值）；规格未给出的数值一律写"待定（需人确认）"。

## 0. 元数据

* **父FEAT**：FEAT-A3-MFCC特征提取

* **优先级**：P1

* **预估总耗时**：2h

* **当前状态**：🟢已收官（2026-08-30：编译零新警告 + 上板 b=7 精确命中 + 评审 Ready with fixes 两 Important 已修复复测）

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）

> 对 A3-01 加窗帧做 FFT 后，经 32 个三角 Mel 滤波器（300Hz\~8kHz）映射为 32 维 Mel 能量，供 A3-03 Log 能量 + DCT-II 使用。

### 1.2 地基触碰前置自检

* [x] 否 → 继续（App 层 DSP 代码，不碰 `firmware/Drivers/` HAL 与 CMSIS-DSP 库源码）

### 1.3 任务边界

| 类型        | 具体内容                                                                                                                         |
| --------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 范围内（可改）   | 新增 `firmware/soundDog/Src/dsp/mel.c`（Mel 滤波器组；目录以 A2-02/A3-01 现有 dsp 结构为准）、对应头文件、`firmware/soundDog/Makefile`（C\_SOURCES 加源） |
| 超出范围（禁止碰） | `firmware/Drivers/` HAL 底层库、CMSIS-DSP 库源码                                                                                    |
| 外部依赖      | A3-01 加窗帧（400 样）、A2-01 CMSIS-DSP（FFT 封装可复用）、A1-04 采样率核实                                                                      |

### 1.4 关键参数（事实包 §3.6 A3-02 + §3.1，禁止自造）

* 滤波器数量：**32 个三角 Mel 滤波器**。

* 频率覆盖：**300Hz \~ 8kHz**（第 1 个滤波器起点 300Hz、第 32 个终点 8kHz）。注：8kHz = Fs/2（奈奎斯特），最后一带贴边，属事实包给定值，如实实现。

* Mel 刻度（标准定义，实现按此计算，**禁止手工编造边界频率表**）：`mel(f) = 2595·log10(1 + f/700)`。

* 输入：A3-01 加窗帧（400 样）→ 需 FFT 转频域；**FFT 点数：512 补零**（A3-01 阶段 2 已定稿：400 样拷入 512 缓冲、高 112 样补零；A3-01 §1.5）。

* 采样率 **Fs = 16kHz**（A1-04 核实后为准）。

* 输出：**32 维 Mel 能量**。

* 打印格式（阶段 2 定稿）：`MEL b=<峰值带号> m=<m0>,<m1>,...,<m31>`（×1 整数截断，BANDS 逗号风格，\~1s 一行节流）。

### 1.5 设计定稿（阶段 2 输出，2026-08-30，按八节模板 + R29 四件套）

#### 一、已确认决策（人确认"按推荐"）

| 决策项        | 定稿                                                                                                              | 来源/理由                               |
| ---------- | --------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| Mel 表生成    | **本地复刻官方公式**（scripts/gen\_mel\_table.py，算法逐字对齐 CMSIS-DSP v1.14.4 Scripts/mfccdata.py 的 melFilterMatrix()，已跑通出表） | 决策 A；numpy/scipy 已装，参数全可配，产物直接 C 数组 |
| 计算节流       | **每帧都算**（设计口径 100 帧/s，CPU \~12%）→ **评审 I-2 实测更正：轮询机制实际处理每窗最新帧 ≈62.5 帧/s**（一窗双出帧跳前帧，CPU ≈7.5%）；A3-03 立项须先决策：接受 62.5/s 或 mfcc.c 改逐帧输出 | 为 A4 连续帧特征直接就绪；实测预算见第五节             |
| 打印格式       | `MEL b=<峰值带> m=32 维逗号串`，1s 节流                                                                                   | BANDS 风格，保留完整分布                     |
| **是否重复加窗** | **不重复加窗**——A3-01 帧已汉明加窗；`arm_mfcc_f32` 源码 L84-90 自带窗+归一化，**整体调用会窗叠窗**，故不整体调官方函数、只复用其分步算法                        | 官方源码核实（R27）                         |
| 归一化        | 不做 1/absmax 归一化（输出原始 Mel 能量；对数域偏移留给 A3-03 按官方 L134 `+1e-6` 再做）                                                  | 分 FEAT 边界                           |
| FFT 实例     | mel.c 内部**独立** `arm_rfft_fast_instance_f32`（512 点），不碰 fft.c/fft\_run（单消费者格局零触碰）                                 | fft.h 头注释约束                         |
| 触发判据       | App 层轮询 `mfcc_frame_count()` 变化才调 mel\_process（128 样块喂入与 160 样出帧不通约）                                            | A3-01 同款机制                          |

#### 二、数据流

```
I2S DMA → audio_queue(128样) → specTask：
  spec_process(pcm256) ──────────────→ SPEC/BANDS/OLED（A2 链路，不动）
  mfcc_feed(pcm256) → 400 样加窗帧 ──→ [新增] 帧计数变化？
                                        └→ mel_process(mfcc_last_frame())
                                             400→512 补零 → rfft(独立实例)
                                             → mag[257] → 32×arm_dot_prod_f32
                                             → mel_out[32] → 串口 MEL 行(1s 节流)
```

#### 三、模块接口（BSP/mel.h）

| API | 签名                                                                      | 职责                                                    |
| --- | ----------------------------------------------------------------------- | ----------------------------------------------------- |
| 初始化 | `int mel_init(void)`                                                    | rfft 512 实例初始化（幂等，任务入口调用）                             |
| 处理  | `int mel_process(const float32_t frame400[400], float32_t mel_out[32])` | 加窗帧→512 补零→rfft→幅度谱→32 维 Mel 能量；返回 0 成功；**A3-03 的输入** |

#### 四、核心机制（无先例可抄部分）

1. **稀疏 Mel 表**（复刻官方）：`mels = linspace(mel(300), mel(8000), 34)`；三角滤波 = `max(0, min(上斜率, 下斜率))`；每带只存 `filtPos/filtLen/非零系数`（totalLen=473），运行时 `arm_dot_prod_f32(mag+filtPos[i], coefs+off, filtLen[i])`——与官方 `arm_mfcc_f32` L120-131 逐字同构。
2. **512 补零边界**：`s_in[0..399]=帧, [400..511]=0`；rfft 输出解包 `tmp[512]=tmp[1], tmp[1]=0` → 复数\[257] → `arm_cmplx_mag_f32(tmp, mag, 257)`（显式 257，边界自证）。
3. **公式等价性**：官方 `1127·ln(1+f/700)` ≡ 规格 `2595·log10(1+f/700)`（2595/log10(e)=1127.0），AC-01 判据无冲突（R28 核查通过）。

#### 五、资源预算（R29 第 3 件）

| 项              | 数值                                                                    | 判据                                    |
| -------------- | --------------------------------------------------------------------- | ------------------------------------- |
| flash（const 表） | +1.91KB（filtPos 128B + filtLen 128B + coefs 473×4B=1892B）+ 代码 \~1.5KB | text 现 161204，余量大                     |
| RAM（bss 静态）    | +\~5.3KB（s\_in 512×4 + s\_tmp 514×4 + s\_mag 257×4 + s\_mel 32×4）     | bss 现 91032 / 192KB，余量足               |
| CPU            | 512 rfft ≈1.0ms + mag 257 + 473 MAC ≈ **1.2ms/帧 × 100 帧/s ≈ 12%**     | specTask 现负载 \~6%，合计 \~18%，168MHz 可承载 |
| 串口带宽           | MEL 行 \~180B × 1 行/s = **+180 B/s**                                   | 现 \~1KB/s @115200，承载无虞                |
| 栈              | 零大数组上栈（全静态）；printf 路径不变                                               | specTask 2KB 栈现状已含 printf             |

#### 六、改动影响地图（R29 第 2 件；CodeGraph callers + grep 双确认，索引重建后 659 文件）

| 变更                            | 实证结论                                                                                                                        |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 新增 `BSP/mel.c/h`              | 纯新文件，零既有调用方                                                                                                                 |
| `App/freertos.c` specTask 内挂接 | `mfcc_feed` 唯一调用方 = SpecTask（codegraph+grep 双确认）；新调用同任务串行，**不新增消费者**，fft\_run 单消费者/USART1 单写者格局不变                           |
| `Makefile` C\_SOURCES         | 登记 `BSP/mel.c`（BUG-002 教训）                                                                                                  |
| **不触碰**                       | fft.c/spectrum.c/oled\_drv.c/displayTask/usart.c——A2 全链路零改动，回归判据=boot 三行 + SPEC 0.5s/BANDS 0.2s 节拍 + OLED 柱图 + MFCC f=100/s |

#### 七、防御设计

* 全程 float32 域计算（无符号陷阱教训）；Mel 能量峰值 \~4e6 < int32 上限（×1 打印安全，A3-01 评审 Minor#2 的饱和问题在此口径下不触发）

* mag 除 DC/Nyquist 外恒非负；dot\_prod 输入有界（filtLen ≤ 34）无溢出路径

* mel\_process 输入 NULL / 帧计数未增时 App 层不调用（防御性检查在 App 挂接处）

* NaN 兜底：若 mag 出 NaN（理论无路径），打印前钳位 → **评审 M-3 记录：ARM 硬浮点 VCVT 对 NaN 得 0、越界饱和，后果良性，钳位不再单独落地（口径放弃，特此记录）**

#### 八、附带成果

* `scripts/gen_mel_table.py` 工具沉淀（A3-03 DCT 表可同法生成）

* R22 索引可信判据再次实证（符号查不到→强制 index→659 文件，假阳性消除）

* `arm_mfcc_f32` 不可整体调用的根因（窗叠窗）落档，A3-03 设计直接继承此结论

#### 附：GitHub 调研（R29 第 1 件）

| 调研对象                                                  | 核心发现                                                                             | 可抄/不可抄（R28 口径）                                   |
| ----------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------ |
| CMSIS-DSP v1.14.4 官方（本地 vendored，源码+Scripts 已读）       | `arm_mfcc_f32` = 归一化+窗+rfft+Mel+log+DCT 一体；Mel 段稀疏 dot\_prod（L120-131）；表生成脚本全参数化 | ✅ Mel 段算法+表生成脚本逐字可抄（硬件无关）；❌ 整体调用（窗叠窗+越 A3-03 边界） |
| hemantnile/stm32-speech-recognition（A3-01 §7.1 #2 已读） | mel 257×26 稀疏阵 + arm\_mat\_mult 实现                                               | ✅ 稀疏滤波思路；❌ 其 26 滤波器参数（本 FEAT 规格=32）              |
| A3-01 §7.1 #3 embedded-ele529（同型 F407）                | FreeRTOS 流水线任务化                                                                  | ✅ 架构佐证（本设计同任务串行更保守）                              |

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）

* **阶段目标**：确认 A3-01 已通过，定位文件，输出影响范围

* [x] 步骤1：确认 A3-01 已通过（🟢 收官：100.8 帧/s 实测 + 评审修复复测，mfcc\_last\_frame() 输出加窗 400 样帧）

* [x] 步骤2：确认 FFT 点数结论（512 补零，A3-01 阶段 2 定稿）

* [x] 步骤3：读 `BSP/mfcc.c`（输出接口 mfcc\_last\_frame/mfcc\_frame\_count）、`BSP/fft.h`（256 点封装不可直接复用→独立实例）；另核实官方 `arm_mfcc_f32.c` 源码与 `Scripts/mfccdata.py`（R27）

* [x] 步骤4：输出影响范围（mel.c/h 新增 + freertos.c 挂接 + Makefile；零触碰 A2 链路）

* **判定标准**：影响范围清单列出，FFT 点数已确认或明确标记阻塞

* **⏸️ 停等：人确认后进入阶段 2**——✅ 用户确认"按推荐方案执行，进入阶段 2 设计"

### 🟢 阶段 2：设计（角色：Dev Agent）

* **阶段目标**：设计滤波器组数据流，产出 §3 傻瓜式操作单

* [x] 步骤1：32 个三角滤波器的边界频率计算方案（**gen\_mel\_table.py 已跑通出表**：滤波器 0 下零点 300Hz（首个非零 bin10=312.5Hz）、滤波器 31 上零点 bin256=8000Hz、filtPos 单调递增 ✓；1kHz→bin32→预期峰值带 7）

* [x] 步骤2：接口设计（mel\_init/mel\_process，§1.5 三）

* [x] 步骤3：产出 §3 操作单（打印格式回填 §3.3）

* **判定标准**：§3 可照做执行；边界频率来源=公式计算，无自造数值

* **⏸️ 停等：人确认后进入阶段 3**

### 🟢 阶段 3：实现（角色：Dev Agent）

* **阶段目标**：写代码，每改完一个文件对照 AC 自检

* [x] 步骤1：新增 `BSP/mel.c` + `BSP/mel.h`（随 mfcc.c 先例落 BSP，§1.3 原文 Src/dsp 以实际为准——评审 M-5 更正）：32 个三角滤波器，300Hz~8kHz，边界频率按 Mel 公式计算

* [x] 步骤2：对加窗帧做 FFT（512 补零，独立 arm_rfft_fast_instance_f32 实例）取幅度谱（257 点，含官方解包三行照抄），与滤波器组稀疏 dot_prod 累加 → 32 维能量

* [x] 步骤3：`Makefile` C_SOURCES 登记 mel.c **+ arm_dot_prod_f32.c**（阶段 3 实测坑：vendored 子集缺该源文件，首链 undefined reference——BUG-002 翻版，已补）

* [x] 步骤4：`firmware\build_and_flash.bat` 编译通过（text 163992/data 108/bss 96640），mel.c 零警告，全量=基线 35

* **判定标准**：编译通过 ✅（size 输出：text +2788 / data +0（const 验证）/ bss +5608 ≈ 预算 5.3KB）

* **⏸️ 停等：人确认后进入阶段 4**——✅ 用户确认"好的，执行烧录验证"

\===== 切换至 Test Agent =====

### 🟢 阶段 4：测试（角色：Test Agent）

* **阶段目标**：独立测试——只看规格（§1.4/§3/§4），不看 Dev 实现细节

* [x] 步骤1：滤波器边界（gen_mel_table.py 三源核查 + 评审员独立重跑表生成器与 mel.c 三表逐值 diff=0）：滤波器 0 下零点 300Hz（首非零 bin10=312.5Hz）、滤波器 31 上零点 bin256=8000Hz 精确、filtPos 单调递增 ✓

* [x] 步骤2：1kHz 纯音（手机音源对麦）：**连续 7 帧 b=7 精确命中预测**（带 7 能量 15.5万 vs 邻带 1.4万/3.3万，10 倍压倒；交叉验证 SPEC peak_bin=16=1000Hz 整）；f=9584→10088 严格 101/s 无丢帧

* [x] 步骤3：静音 vs 扰动对比：e=4万（静音底噪）→1.8亿（强声）数量级摆动，b 值 30→5/7→30 严格跟随（环境底噪因预加重高带占优属数学预期）

* [x] 步骤4：CI = build.bat（编译校验通过，见阶段 3）

* **判定标准**：AC-01/AC-02 有实测证据 ✅（记录实际值见上）

* **⏸️ 停等：人确认后进入阶段 5**——✅ 用户确认"好的，进入阶段 5"

\===== 切换至 Review Agent =====

### 🟢 阶段 5：审查（角色：Review Agent）

* **阶段目标**：独立审查——安全检查、代码质量、规范合规、AC 逐条打勾

* **评审结论（2026-08-30）：Ready with fixes → 修复后通过**。评审员三重独立验证：官方 v1.14.4 源码逐行比对（解包三行逐字一致）+ 本地重跑表生成器与 mel.c 三表**逐值 diff=0**（473 系数）+ 边界索引手工复算（末带 [222,255] 无越界）。发现：**I-1** MEL/BANDS 行 snprintf 截断保护失效（len 超限时补行尾反成越界写；现实不可达但防护自证失效）→ 已修（两处钳位）；**I-2** "每帧都算 100/s"实际为"每窗最新帧 ≈62.5/s"（256 样窗与 160 样帧移不通约，一窗双帧跳前帧）→ 注释/文档口径更正 + A3-03 立项决策项；M-2 帧长 `_Static_assert` 顺手落地。另证实官方 arm_mfcc_f32 自身传 fftLen 给 arm_cmplx_mag_f32 属越界读，本实现 257 更安全且等价。
* 修复复测：text 164008（+16B），零新警告；烧录 Verified OK；10s 回归 boot 三行逐字 + f=98→701 严格 100/s + MEL 行正常（评审 M-7 的 f=99/s 稳态担忧未现，101/s）。
* 全部通过 → ✅ 回填「维护与调试」+ 父FEAT项目表 + INDEX + 维护地图

## 3. 傻瓜式操作单（照做即可）

### 3.0 物品/工具清单（动手前先打勾，缺一不可）

* [ ] 开发机（mingw32-make + arm-none-eabi-gcc 环境）

* [ ] 核心板（A1/A2 链路已验证通过）

* [ ] ST-Link（SWD：SWDIO/SWCLK/GND/3.3V 四线）

* [ ] USB-TTL（CH340）+ SSCOM 串口助手（115200，COM 以设备管理器为准）

* [ ] 5V 电源：电脑 USB 口或非快充 5V 适配器（**供电红线：严禁手机快充头**）

### 3.1 编译（阶段 3 执行）

1. 打开 `firmware` 目录，运行 `build.bat`。
2. 期望：编译**无错误退出**，**无新增警告**（记录警告数：\_\_\_）。
3. 失败 → 查 §3.4 故障速查表。

### 3.2 烧录（阶段 3/4 执行；插拔 ST-Link 前先断电源）

1. 接好 ST-Link 四线，运行 `firmware\build_and_flash.bat`。
2. 期望：OpenOCD 输出 `wrote ... bytes` / `Verified OK`（记录：\_\_\_）。
3. **失败兜底**：OpenOCD 连不上 → 查四线/驱动；仍不行 → `firmware\flash_isp.bat`（BOOT0+USART1，HANDOFF §4.3）。

### 3.3 串口核对（阶段 4 执行）

1. USB-TTL 接核心板：TX↔RX **交叉**、GND 共地；SSCOM 115200。
2. 按复位键，期望依次出现三行：`SoundDog boot OK` → `I2S_DRV_Init ret=0` → `I2S DMA started`。
3. 观察 Mel 能量打印（格式定稿见 §1.5）：`MEL b=<峰值带号> m=<m0>,...,<m31>`（\~1s 一行），期望**维度 = 32**（实测：32 维 ✓，1s 节拍精确，2026-08-30）。
4. 静音 5 秒：32 维整体能量小（实测底噪峰值带 18\~30 高带区、单带 100\~6000——预加重底噪特征，数学预期）。
5. 播放 1kHz 正弦（A2-02 同款声源）5 秒：能量应集中在 1kHz 附近 Mel 带（**实测峰值带号 b=7 连续 7 帧、数值 15.5万 vs 邻带 1.4万/3.3万**；SPEC peak_bin=16 交叉验证 1000Hz 整）。
6. 吹气 5 秒：能量分布明显展宽/变化（实测扰动 e=4万→1.8亿 摆动，b 30→5/7→30 严格跟随，低带 26k\~71k 显著占优）。

### 3.4 故障速查表

| 现象        | 可能原因                           | 处理                                     |
| --------- | ------------------------------ | -------------------------------------- |
| 32 维全部为 0 | 输入帧全 0 / FFT 未接 / 取数掩码错        | 回溯 A3-01 加窗帧输出；查 BUG-002 取数            |
| 输出维度 ≠32  | 滤波器个数实现错                       | 核对 32 个三角滤波器                           |
| 峰值带号与预期不符 | 采样率不是 16kHz / FFT 点数换算错        | A1-04 核实采样率；按所选 FFT 点数重算 bin           |
| 高频带能量恒为 0 | 滤波器高频端点贴奈奎斯特（8kHz=Fs/2）/ 声源无高频 | 属事实包给定边界，如实核对；换宽带声源（吹气）复测              |
| NaN / 溢出  | float32 / FPU                  | 查 `-mfpu=fpv4-sp-d16 -mfloat-abi=hard` |
| 编译链接失败    | Makefile 漏加源（BUG-002 教训）       | C\_SOURCES 加 `dsp/mel.c` 重编            |

## 4. 验收标准（12 条）

### 功能验收

* [x] AC-01 32 个三角 Mel 滤波器，覆盖 300Hz\~8kHz：第 1 个起点 300Hz、第 32 个终点 8kHz，频点单调递增（实测记录于 §3.3；评审员重跑表生成器逐值 diff=0）

* [x] AC-02 1kHz 纯音能量集中在 1kHz 附近 Mel 带（**实测 b=7 连续 7 帧，带 7 能量 15.5万 vs 邻带 1.4万/3.3万**），输出维度恒为 32、非全 0

### 代码质量

* [x] AC-03 编译通过（`build.bat` 无错误退出；text 164008/data 108/bss 96640）

* [x] AC-04 无新增警告（mel.c 零警告；全量=基线 35，唯一显示项为已知 `__FPU_PRESENT redefined`）

* [x] AC-05 文档已更新（本 FEAT §5 维护章节 + 父FEAT项目表文件列回填 + INDEX + 维护地图）

### 安全红线

* [x] AC-06 无硬编码密钥 —— **N/A**（嵌入式 DSP 代码，无密钥/凭据概念）

* [x] AC-07 错误处理完善：滤波器组索引/边界越界防护（评审手工复算末带 [222,255] 无越界；I-1 snprintf 越界写已修 + BANDS 同款闭环；`_Static_assert` 帧长耦合检查落地）

* [x] AC-08 用户输入已校验 —— **N/A**（无外部用户输入；输入为内部加窗帧）

### 测试与边界

* [x] AC-09 新增测试自动运行 —— **N/A**（项目无自动化测试框架，验证走 §3 串口核对）

* [x] AC-10 回归：A1-02 链路与 A2-02 频谱输出不被破坏（boot 三行逐字 + SPEC/BANDS/OLED 节拍 + f=100/s 三轮实测不变）

* [x] AC-11 测试覆盖率 ≥80% —— **N/A**（无覆盖率工具）

* [x] AC-12 按 FEAT 模板执行：5 阶段按序、角色声明、⏸️ 停等、日志完整

## 5. 维护与调试（阶段5通过后回填）

### 5.1 影响文件

| 文件                                                | 改动类型 | 说明                       |
| ------------------------------------------------- | ---- | ------------------------ |
| `firmware/soundDog/BSP/mel.c`（评审 M-5：随 mfcc.c 先例落 BSP，§1.3 原文 Src/dsp 以实际为准）                 | 新增   | 32 三角 Mel 滤波器组 + 独立 512 rfft 实例 + 473 系数稀疏表（const）           |
| `firmware/soundDog/BSP/mel.h`（评审 M-5 同上） | 新增   | mel_init/mel_process 接口                 |
| `firmware/soundDog/App/freertos.c`                      | 修改   | specTask 挂接（帧计数轮询）+ MEL 行 1s 节流 + 评审 I-1/I-2/M-2 修复 |
| `firmware/soundDog/Makefile`                      | 修改   | C\_SOURCES 加 `BSP/mel.c` + `BasicMathFunctions/arm_dot_prod_f32.c` |
| `scripts/gen_mel_table.py`                        | 新增   | Mel 表生成器（复刻官方 mfccdata.py，A3-03 DCT 表可同法） |

### 5.2 调试要点

* 峰值带号偏移 → A1-04 采样率核实 + FFT 点数换算

* 全 0 → 回溯 A3-01 / BUG-002 取数

### 5.3 测试入口

* `firmware\build.bat` + `firmware\build_and_flash.bat` + SSCOM 115200 + 1kHz 正弦声源（对应 AC-01/AC-02）

## 6. 执行日志

| 时间         | 阶段          | 角色        | 摘要                                                                                                                                                                                                                    | 遇阻                                                        |
| ---------- | ----------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| 2026-08-30 | 阶段 1（准备+预研） | Dev Agent | 依赖确认（A3-01 🟢 / 512 补零已定 / fft.c 256 点不可复用→独立实例）；官方 arm\_mfcc\_f32 源码核实（**不可整体调用：自带窗+归一化会窗叠窗**，Mel 段算法可逐段复用）；官方 Scripts/mfccdata.py 算法全文到手（参数全可配）；公式等价性验证（1127·ln ≡ 2595·log10）                                       | GenMFCCDataForCPP.py raw 拉取 502（算法已在 mfccdata.py 中拿到，不影响） |
| 2026-08-30 | 阶段 2（设计）    | Dev Agent | 三决策定稿（人确认"按推荐"）：本地复刻公式 / 每帧算 / MEL 全 32 维打印；**gen\_mel\_table.py 跑通出表**（totalLen=473、边界核查全过、1kHz→带 7 预测）；设计定稿 §1.5（八节 + R29 四件套：GitHub 调研 3 项 / 影响地图 codegraph+grep 双确认 / 资源预算 flash+1.91KB const、bss+5.3KB、CPU\~12%） | codegraph 符号查不到（索引过期）→ R22 强制重建（659 文件）后双确认通过             |
| 2026-08-30 | 阶段 3（实现）    | Dev Agent | mel.c/h 落地（BSP 先例）+ freertos.c 挂接（帧计数轮询 + MEL 1s 节流）+ Makefile 登记；size：text 163992（+2788）/ data 108（+0，const 验证）/ bss 96640（+5608）；mel.c 零警告、全量=基线 35 | 首链 undefined reference to arm\_dot\_prod\_f32——vendored 子集缺源文件（BUG-002 翻版），补登记后通过 |
| 2026-08-30 | 阶段 4（测试）    | Test Agent | 烧录 Verified OK；三轮串口实测：boot 三行逐字 ✓ / f=98→1205 严格 100/s 无丢帧（坑 3 判据过）/ 1kHz 纯音 **b=7 连续 7 帧精确命中**（带 7 能量 15.5万 vs 邻带 1.4万/3.3万，SPEC peak\_bin=16 交叉验证）/ 扰动跟随 ✓ / A2 回归全过 | 首抓 15s 首行 f=1810 为 reset 前旧固件残余（剔除后干净），非丢帧 |
| 2026-08-30 | 阶段 5（审查）    | Review Agent | 独立评审 **Ready with fixes**：官方源码逐行比对 + 表再生成逐值 diff=0 + 边界手工复算全对；I-1 snprintf 截断保护失效（MEL+BANDS 两处钳位修复，+16B 复测回归过）；I-2 实际 62.5 帧/s 口径更正（A3-03 决策项）；M-2 \_Static\_assert 落地；官方 arm\_mfcc\_f32 自身 mag 越界读被证实（本实现 257 更安全）。修复复测：烧录 + 10s 回归全过，A3-02 收官 | 无 |

