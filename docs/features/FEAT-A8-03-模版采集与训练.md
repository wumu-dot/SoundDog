# FEAT-A8-03-模版采集与训练

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行 + 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。
> 事实来源：`.sop-agent规格.md` §3 + 父 FEAT-A8（禁止自造数值；未覆盖项写"待定（需人确认）"）。

## 0. 元数据
- **父FEAT**：FEAT-A8-上位机
- **优先级**：P1
- **预估总耗时**：5h（算法部分可独立提前开发，不需硬件）
- **当前状态**：🔴未开始（傻瓜式操作单已备好；算法部分不阻塞，可先于 A5 开发）

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）
> PC 端**采集**板端 MFCC 特征（13 维）→ **K-means(K=16) 训练** → 导出**模版文件**，供 A4-01 比对使用（与 A4-01 共用同一算法设计）。

### 1.2 地基触碰前置自检
- [x] 否 → 继续（PC 端算法 + 串口采集，不碰 `firmware/Drivers/`）

### 1.3 任务边界
| 类型 | 具体内容 |
|------|----------|
| 范围内（可改） | 新建上位机 `ml/` 模块（采集、K-means、模版文件读写）、训练脚本 |
| 超出范围（禁止碰） | `firmware/Drivers/` HAL；板端 MFCC 实现（A3 范畴，本 FEAT 只消费其输出） |
| 外部依赖 | A3 板端 MFCC 数据流（13 维）、**Anaconda Python 3.13.5（numpy/scipy/scikit-learn 1.6.1 已装，事实包 §3.7）**；联调依赖 A8-01 通信层（采集实数据） |

### 1.4 关键参数与接口（事实包 §3 + 父 FEAT-A8，禁止自造）
- **特征**：**13 维 MFCC**（事实包 A3-03/A3-04）。
- **训练**：**K-means，K=16**（事实包 A4-01）；**用 scikit-learn 1.6.1 的 `KMeans`（已装，事实包 §3.7）**，不再自实现。
- **数据来源**：板端 A3-04 串口输出 13 维特征行（格式**待定需人确认**）；或预先保存的数据文件（供离线开发）。
- **输出**：模版文件（16×13 簇中心，格式**待定需人确认**；供 A4-01 加载）。
- **运行环境**：**Anaconda `C:\Users\wumu2\anaconda3\python.exe`（3.13.5）**；numpy 2.1.3 / scikit-learn 1.6.1 已装，无需补装；⚠️ 勿用 conda 命令。
- **待定（需人确认）**：采集时长/样本量、归一化方式、模版文件格式、K-means 初始化（random vs k-means++，sklearn 默认 k-means++）、随机种子。

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）
- **读什么**：`A4-01` 文档（模版生成约定）、`A3-04` 文档（特征输出格式）、父 `FEAT-A8`
- [ ] 步骤1：确认 A3-04 特征输出格式（13 维行格式待定需人确认）
- [ ] 步骤2：确认 K-means 实现方式：**用 scikit-learn 1.6.1 `KMeans`（已装，事实包 §3.7）**，记录版本
- [ ] 步骤3：确认离线数据文件是否存在（无 → 用模拟数据先开发算法）
- **判定标准**：数据格式已明确（或明确标注待定）、实现方式已确认、离线数据源已确认
- **⏸️ 停等：人确认后进入阶段 2**

### 🔵 阶段 2：设计（角色：Dev Agent）
- **阶段目标**：设计采集与训练流程，产出傻瓜式操作单（§3）
- [ ] 步骤1：设计 K-means 算法实现（K=16、迭代收敛条件、初始化方式）
- [ ] 步骤2：设计模版文件格式（16×13，含版本/校验，格式待定需人确认）
- [ ] 步骤3：产出 §3 操作单（离线训练 + 实采训练 + 故障表）
- **判定标准**：算法参数已定、文件格式已产出、待定项已列全
- **⏸️ 停等：人确认后进入阶段 3**

### 🟠 阶段 3：实现（角色：Dev Agent）
- **阶段目标**：实现 ml/ 模块 + 训练脚本，离线自测
- [ ] 步骤1：实现 K-means（K=16）+ 数据加载/归一化
- [ ] 步骤2：实现模版文件写入/读取（含校验）
- [ ] 步骤3：`python -m py_compile` + 用模拟数据训练（AC-03：收敛、K 簇非空）
- **判定标准**：语法通过、模拟数据训练收敛（WCSS 下降、无空簇）
- **⏸️ 停等：人确认后进入阶段 4**

===== 切换至 Test Agent =====

### 🟣 阶段 4：测试（角色：Test Agent）
- **阶段目标**：独立验证——按 §3 操作单实测，核对 §4 验收标准
- [ ] 步骤1：按 §3.2 离线训练（已知分布数据 → K=16 簇中心合理）（AC-01）
- [ ] 步骤2：按 §3.3 实采训练（板端真实 MFCC → 模版文件可读）（AC-02；依赖 A8-01/A3，未就绪则 ⏸️ 停等）
- [ ] 若失败 → 查 §3.4 故障表，退回阶段 3
- **判定标准**：AC-01/02 有实际记录数值
- **⏸️ 停等：人确认后进入阶段 5**

===== 切换至 Review Agent =====

### 🔴 阶段 5：审查（角色：Review Agent）
- **阶段目标**：独立审查——AC 逐条打勾、算法与 A4-01 约定一致
- [ ] 步骤1：核对 §4 全部 AC 勾选与记录值（收敛、簇数、文件读回）
- [ ] 步骤2：核对模版文件格式/维度（16×13）与 A4-01 输入约定一致
- **判定标准**：AC 全部通过且记录值完整
- 全部通过 → ✅ 回填「维护与调试」+ 父FEAT项目表 + INDEX + 维护地图
- 未通过 → 退回对应阶段并附意见
- **⏸️ 停等：人确认 Review 通过后归档**

## 3. 傻瓜式操作单（照做即可）

### 3.0 物品/工具清单（动手前先打勾，缺一不可）
- [ ] PC（Windows）+ **Anaconda Python 3.13.5**（`C:\Users\wumu2\anaconda3\python.exe`）+ numpy 2.1.3 / scikit-learn 1.6.1（已装，事实包 §3.7）
- [ ] （实采联调时）核心板 A3-04 在跑 + A8-01 通信层 + 5V 电源

### 3.1 离线训练验证（阶段 4 执行；不需硬件，可提前做）
1. 生成模拟数据：3 个高斯簇各 200 个 13 维样本（共 600 条）。
2. 运行 K-means(K=16) → 期望**收敛**（迭代次数记录：___；WCSS 不再下降）。
3. 检查：16 个簇**全部非空**（空簇数：___，期望 0）。
4. 导出模版文件 → 读回 → 期望 **16×13 维度一致、逐值一致**。记录：一致 / 不一致

### 3.2 实采训练验证（阶段 4 执行；依赖 A8-01/A3，未就绪则 ⏸️ 停等）
1. 板端运行 A3-04 特征输出；上位机连接 CH340（115200），开始采集（时长/样本量：待定需人确认，记录：___）。
2. 采集完成后运行 K-means(K=16) → 期望收敛。
3. 导出模版文件（记录路径：___）→ 供 A4-01 加载。

### 3.3 故障速查表
| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 训练不收敛 | 数据全相同 / 迭代次数不足 | 检查数据方差；加大迭代/收敛阈值（待定） |
| 有空簇 | K 过大 / 初始化不当 | 用 K-means++ 初始化；降低 K（待定需人确认） |
| 采集数据为空 | COM 没连上 / A3-04 未在跑 | 先跑 A8-01 验证；SSCOM 确认特征行存在 |
| 模版文件读回失败 | 格式/校验不一致 | 核对版本/校验实现（格式待定需人确认） |
| 特征维度不是 13 | A3-04 格式解析错位 | 对照 A3-04 定稿格式逐字段核对 |

## 4. 验收标准（12 条）

### 功能验收
- [ ] AC-01 离线训练收敛、16 簇非空、模版文件读回 16×13 逐值一致（§3.1 记录）
- [ ] AC-02 实采训练产出模版文件，可被 A4-01 加载（§3.2 记录；依赖就绪后验证）
- [ ] AC-03 向后兼容：PC 端独立模块，不影响板端固件

### 代码质量
- [ ] AC-04 语法检查通过（`python -m py_compile`）
- [ ] AC-05 数值稳健：空簇/全同数据有明确处理（不崩溃）
- [ ] AC-06 文档已更新（本 FEAT + 父 FEAT-A8 回填）

### 安全红线
- [ ] AC-07 N/A：无密钥
- [ ] AC-08 N/A：无用户输入注入面（数据文件按格式解析）

### 测试与边界
- [ ] AC-09 离线用例已执行（§3.1）
- [ ] AC-10 N/A：无自动化测试基建（待定需人确认）
- [ ] AC-11 N/A：手动验证，覆盖率不适用
- [ ] AC-12 按 FEAT 模板执行（本文件即模板产物）

## 5. 维护与调试（阶段5通过后回填）

### 5.1 影响文件
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `host_app/ml/kmeans.py` | 新增 | K-means(K=16) 实现 |
| `host_app/ml/template_io.py` | 新增 | 模版文件读写（16×13） |
| `host_app/ml/train.py` | 新增 | 训练入口脚本 |
| `host_app/ml/data_collect.py` | 新增 | 串口特征采集（联调用） |

### 5.2 调试要点
- 训练过程打印：每轮 WCSS、簇大小分布；空簇即时告警
- 模版文件头打印：版本/维度/校验值

### 5.3 测试入口
- §3.1 离线训练脚本（对应 AC-01）；§3.2 实采（对应 AC-02）

## 6. 执行日志

| 时间 | 阶段 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| | | | | |
