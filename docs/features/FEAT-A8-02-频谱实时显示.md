# FEAT-A8-02-频谱实时显示

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行 + 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。
> 事实来源：`.sop-agent规格.md` §3 + 父 FEAT-A8（禁止自造数值；未覆盖项写"待定（需人确认）"）。

## 0. 元数据
- **父FEAT**：FEAT-A8-上位机
- **优先级**：P1
- **预估总耗时**：4h
- **当前状态**：🔴未开始（傻瓜式操作单已备好，依赖 A8-01 通信层通过后开跑）

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）
> 读取板端 A2-04 输出的 **32 频带能量数组**（一行 32 整数、~200ms 节流），在 PC 上位机 **实时绘制频谱**（柱状/波形），数值与串口数据一致、UI 不卡顿。

### 1.2 地基触碰前置自检
- [x] 否 → 继续（PC 端 GUI 应用，不碰 `firmware/Drivers/`）

### 1.3 任务边界
| 类型 | 具体内容 |
|------|----------|
| 范围内（可改） | 新建上位机 `gui/` 模块（频谱窗口、数据队列、绘图刷新） |
| 超出范围（禁止碰） | `firmware/Drivers/` HAL；板端 A2-04 格式（本 FEAT 只做 PC 端接收解析，格式以 A2-04 定稿为准） |
| 外部依赖 | A8-01 通信层、A2-04 频谱输出（板端）、**matplotlib 3.10.0 + tkinter（Anaconda 自带，零安装）** |

### 1.4 关键参数与接口（事实包 §3 + 父 FEAT-A8，禁止自造）
- **数据源**：板端 A2-04 串口输出，**一行 32 个整数**（能量），**~200ms 节流**（事实包 A2-04）。
- **UI**：**tkinter 主窗口 + matplotlib 绘图组件**（`FigureCanvasTkAgg`）；刷新周期 ~200ms（与数据节流一致，避免队列堆积）。
- **绘图库**：**matplotlib 3.10.0（Anaconda 已装，事实包 §3.7）**，走 TkAgg 后端；不再引入 PySide6（6.11 DLL 加载失败已放弃，事实包 §3.7）。
- **运行环境**：**Anaconda Python 3.13.5**（`C:\Users\wumu2\anaconda3\python.exe`）；依赖全自带，**无需补装**；⚠️ 勿用 conda 命令。
- **数据流**：A8-01 comm 收到频谱帧 → 解析 32 整数 → 入队（线程安全）→ tkinter `after()` 定时取队绘制。
- **待定（需人确认）**：Y 轴归一化方式、颜色方案、是否叠加波形与柱状双视图。

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）
- **读什么**：`A8-01` 文档（comm 接口）、`A2-04` 文档（串口输出格式）、父 `FEAT-A8`
- [ ] 步骤1：确认 A8-01 已通过（comm 可收发）
- [ ] 步骤2：确认板端 A2-04 已在跑（SSCOM 可见 32 整数行）
- [ ] 步骤3：确认 Python 环境（matplotlib + tkinter 可用，`python -c "import matplotlib; import tkinter"`）
- **判定标准**：A8-01/A2-04 状态已核实、环境已确认
- **⏸️ 停等：人确认后进入阶段 2**

### 🔵 阶段 2：设计（角色：Dev Agent）
- **阶段目标**：设计数据流与 GUI 结构，产出傻瓜式操作单（§3）
- [ ] 步骤1：设计数据流（comm 回调 → 线程安全队列 → GUI 定时刷新）
- [ ] 步骤2：确定绘图组件与刷新策略（~200ms，队列空时跳过不堆积）
- [ ] 步骤3：产出 §3 操作单（环境搭建 + 联调 + 故障表）
- **判定标准**：数据流图已产出、刷新策略已定、待定项已列全
- **⏸️ 停等：人确认后进入阶段 3**

### 🟠 阶段 3：实现（角色：Dev Agent）
- **阶段目标**：写 GUI 模块，本地运行自测
- [ ] 步骤1：搭建 tkinter 主窗口 + matplotlib 绘图组件（`FigureCanvasTkAgg`）
- [ ] 步骤2：实现 comm 数据接入（队列 + 解析 32 整数）
- [ ] 步骤3：`python -m py_compile` 语法检查 + 本地伪造数据流自测（AC-03）
- **判定标准**：语法通过、本地自测窗口能画出变化的柱状图
- **⏸️ 停等：人确认后进入阶段 4**

===== 切换至 Test Agent =====

### 🟣 阶段 4：测试（角色：Test Agent）
- **阶段目标**：独立验证——按 §3 操作单联调实测，核对 §4 验收标准
- [ ] 步骤1：按 §3.2 联调（接板端真实数据，吹气看频谱变化）（AC-01）
- [ ] 步骤2：按 §3.3 长时间运行（10 分钟）观察卡顿/队列堆积（AC-02）
- [ ] 若失败 → 查 §3.4 故障表，退回阶段 3
- **判定标准**：AC-01/02 有实际观察记录
- **⏸️ 停等：人确认后进入阶段 5**

===== 切换至 Review Agent =====

### 🔴 阶段 5：审查（角色：Review Agent）
- **阶段目标**：独立审查——AC 逐条打勾、数据格式与 A2-04 一致
- [ ] 步骤1：核对 §4 全部 AC 勾选与记录值（频谱变化、卡顿观察）
- [ ] 步骤2：核对 32 整数解析与 A2-04 串口格式逐字段一致
- **判定标准**：AC 全部通过且记录值完整
- 全部通过 → ✅ 回填「维护与调试」+ 父FEAT项目表 + INDEX + 维护地图
- 未通过 → 退回对应阶段并附意见
- **⏸️ 停等：人确认 Review 通过后归档**

## 3. 傻瓜式操作单（照做即可）

### 3.0 物品/工具清单（动手前先打勾，缺一不可）
- [ ] PC（Windows）+ **Anaconda Python 3.13.5**（`C:\Users\wumu2\anaconda3\python.exe`，事实包 §3.7）
- [ ] matplotlib + tkinter 可用（`python -c "import matplotlib, tkinter"`；两者 Anaconda 自带，**无需补装**）
- [ ] A8-01 通信层已通过
- [ ] 核心板（A2-04 频谱输出在跑，SSCOM 可见 32 整数行）
- [ ] 5V 电源（非快充）+ USB-TTL

### 3.1 环境确认（阶段 1 执行）
1. `& C:\Users\wumu2\anaconda3\python.exe -c "import matplotlib, tkinter; print('GUI deps OK')"` → 期望 `GUI deps OK`（记录：___）
2. `& C:\Users\wumu2\anaconda3\python.exe -c "import matplotlib; print(matplotlib.__version__)"` → 记录：___（期望 3.10.0）
3. SSCOM 打开板端调试口，确认每 ~200ms 出现一行 32 个整数。记录样本行：__________

### 3.2 联调测试（阶段 4 执行）
1. 启动上位机，选择 CH340 COM 口（115200），点击"连接"。
2. 期望：频谱窗口开始**实时刷新 32 个柱/波形**。
3. 对麦克风**吹气/敲击** → 期望对应频段柱高明显变化。记录：变化频段：___，幅度变化：___
4. 数值与 SSCOM 上同帧数据**一致**（随机抽 3 帧对比）。记录：一致 / 不一致

### 3.3 长时间运行（阶段 4 执行）
1. 持续运行 **10 分钟**（期间正常吹气）。
2. 期望：UI 不卡顿、刷新不掉帧、内存不持续增长。记录：卡顿：无/有；异常：___

### 3.4 故障速查表
| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 窗口空白无数据 | COM 没连上 / 数据格式不匹配 | 检查连接状态；对照 A2-04 格式核对解析 |
| 柱状图不变化 | 队列堆积/刷新停住 / 板端无数据 | 看 SSCOM 是否仍有 32 整数行；检查定时器是否运行 |
| 数值对不上 | 解析错位（少读/多读字节） | 对照一行 32 整数逐字段核对 |
| UI 卡顿 | 刷新过快 / 绘图库性能差 | 刷新改 ~200ms；换 pyqtgraph（待定需人确认） |
| 内存持续增长 | 队列无限堆积 | 确认队列有上限/消费速率≥生产速率 |

## 4. 验收标准（12 条）

### 功能验收
- [ ] AC-01 频谱窗口实时刷新 32 频带，吹气对应频段柱高明显变化，数值与 SSCOM 一致（§3.2 记录）
- [ ] AC-02 10 分钟运行 UI 不卡顿、无队列堆积（§3.3 记录）
- [ ] AC-03 向后兼容：PC 端独立模块，不影响板端固件

### 代码质量
- [ ] AC-04 语法检查通过（`python -m py_compile`）
- [ ] AC-05 线程安全：comm 回调与 GUI 刷新无竞态（队列+定时器）
- [ ] AC-06 文档已更新（本 FEAT + 父 FEAT-A8 回填）

### 安全红线
- [ ] AC-07 N/A：无密钥（本地 GUI）
- [ ] AC-08 N/A：无用户输入注入面（数据按协议解析）

### 测试与边界
- [ ] AC-09 长跑用例已执行（§3.3 10 分钟）
- [ ] AC-10 N/A：无自动化测试基建（待定需人确认）
- [ ] AC-11 N/A：手动验证，覆盖率不适用
- [ ] AC-12 按 FEAT 模板执行（本文件即模板产物）

## 5. 维护与调试（阶段5通过后回填）

### 5.1 影响文件
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `host_app/gui/main_window.py` | 新增 | 主窗口 + 频谱绘制 |
| `host_app/gui/spectrum_view.py` | 新增 | 32 频带视图组件 |
| `host_app/core/data_queue.py` | 新增 | 线程安全数据队列 |

### 5.2 调试要点
- 数据链路逐级打点：comm 收到 → 队列长度 → 绘制帧号，定位断点
- 内存观察：任务管理器确认运行 10 分钟内存平稳

### 5.3 测试入口
- §3.2 联调 + §3.3 长跑（对应 AC-01/02）

## 6. 执行日志

| 时间 | 阶段 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| | | | | |
