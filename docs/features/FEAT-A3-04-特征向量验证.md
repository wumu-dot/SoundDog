# FEAT-A3-04-特征向量验证

> **AI 执行规则**：必须按阶段顺序执行，每个阶段开头声明角色。严禁跳跃、越界、自审自判。
> 阶段间停等：每完成一个阶段，停下来，输出结果，等人说"继续"。
> 阶段5通过后：回填父FEAT项目表该行 + 更新 `docs/features/INDEX.md` + `docs/features/维护地图.md`。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。
> 事实来源：`.sop-agent规格.md` §3（禁止自造数值）；规格未给出的数值一律写"待定（需人确认）"。

## 0. 元数据

* **父FEAT**：FEAT-A3-MFCC特征提取

* **优先级**：P1

* **预估总耗时**：1.5h

* **当前状态**：🟢已收官（2026-08-30：三态证据齐 + e 钳位清偿 + R23 独立评审 Ready with fixes 全修复）

## 1. 总体目标与硬边界

### 1.1 核心目标（一句话）

> 串口打印 13 维 MFCC，用静音 / 1kHz 纯音 / 吹气三种已知信号验证特征向量与信号特征吻合，为 A4 模版比对提供可信输入。

### 1.2 地基触碰前置自检

* [x] 否 → 继续（仅补打印/验证，不碰 `firmware/Drivers/` HAL 与 CMSIS-DSP 库源码）

### 1.3 任务边界

| 类型        | 具体内容                                                                       |
| --------- | -------------------------------------------------------------------------- |
| 范围内（可改）   | `dsp/mfcc_dct.c` 或调用处的串口打印（如需补打印）、`firmware/soundDog/Makefile`（如需加源）       |
| 超出范围（禁止碰） | `firmware/Drivers/` HAL 底层库、CMSIS-DSP 库源码、A3-01\~03 算法参数（如需改动 → 退回对应子FEAT） |
| 外部依赖      | A3-03 通过（13 维输出）、已知声源（1kHz 正弦，A2-02 同款）、SSCOM                              |

### 1.4 关键参数（事实包 §3.6 A3-04 + §3.1，禁止自造）

* **串口打印 13 维 MFCC**，与已知信号特征吻合（事实包 A3-04）。

* 已知信号源：A2-02 的 **1kHz 正弦**测试信号 / 吹气（宽带噪声）/ 静音。

* 期望特征（定性，记录实测）：

  * 静音 → 13 维整体能量低；

  * 1kHz 纯音 → 低频 Mel 带对应系数明显（能量集中于低频）；

  * 吹气/噪声 → 高频带分量出现，与纯音分布明显不同。

* **MFCC 数值范围：待定（需人确认）**（A4 用 K-means(K=16) 建模时确定正常范围；本 FEAT 只记录实测值，不做阈值判定）。

* 打印节流：**待定（需人确认）**（可选参考 A2-04 的 \~200ms 节流，事实包 §3.6 A2-04）。

* 采样率 **Fs = 16kHz**（A1-04 核实后为准）。

## 2. 执行路线图（5 阶段）

### 🟢 阶段 1：准备（角色：Dev Agent）✅ 完成（2026-08-30）

* **阶段目标**：确认 A3-03 已通过，确认打印现状，输出影响范围

* [x] 步骤1：确认 A3-03 已通过（13 维输出正常，记录在 A3-03 日志）

* [x] 步骤2：确认固件已打印 13 维（`MFCC13 c=...` A3-03 已实现）→ 无补打印代码；打印节流"待定"项已被上游 A3-02 帧率修正闭环为 1s

* [x] 步骤3：读 `dsp/mfcc_dct.c` 输出接口与主循环调用处（实际路径 BSP/mfcc\_dct.c；消费点 freertos.c mel\_frame\_cb→s\_mfcc13）

* [x] 步骤4：影响范围：**无算法改动** + 唯一代码改动候选=e 钳位（人确认执行）

* **判定标准**：影响范围清单列出（改动/无改动二选一明确）✅

* **⏸️ 停等：人确认后进入阶段 2** ✅（人认可 + 指示执行 e 钳位）

### 🔵 阶段 2：设计（角色：Dev Agent）✅ 完成（2026-08-30，方案 A'）

* **阶段目标**：设计验证方案，产出 §3 傻瓜式操作单

* [x] 步骤1：验证顺序 静音→1kHz→吹气；记录格式=MFCC13 行

* [x] 步骤2：无补打印；节流 1s 沿用（A3-03 定稿）

* [x] 步骤3：§3 操作单已产出（含方案 A' 数据口径注记）

* **判定标准**：§3 可照做执行 ✅

* **⏸️ 停等：人确认后进入阶段 3** ✅（人认可方案 A'）

### 🟠 阶段 3：实现（角色：Dev Agent）✅ 完成（2026-08-30）

* **阶段目标**：按需补打印代码并编译烧录

* [x] 步骤1：无补打印（A3-03 已有）；e 钳位 1 行落地（mfcc.c:103-105）

* [x] 步骤2：无需改 Makefile（无新源文件）

* [x] 步骤3：`firmware\build.bat` → **Build OK 零警告**

* [x] 步骤4：烧录 → **Verified OK**；text +20B；8s 冒烟：boot/节拍/e 正常（钳位未误触发）

* **判定标准**：编译烧录成功（记录输出）✅

* **⏸️ 停等：人确认后进入阶段 4** ✅（方案 A' 人确认后免除重复采集）

\===== 切换至 Test Agent =====

### 🟣 阶段 4：测试（角色：Test Agent）✅ 完成（2026-08-30，方案 A' 口径）

* **阶段目标**：独立测试——按 §3 操作单采集三种信号，逐条核对 §4 验收标准

* [x] 步骤1：静音 10s：记录 13 维（期望整体能量低、无 NaN）→ §3.3 ✅（5 行采样，无 NaN，f=100/s）

* [x] 步骤2：1kHz 纯音 10s：记录 13 维（期望低频带系数明显、数值稳定）→ §3.4 ✅（c0 标准差 \~30 + MEL b=7 命中 + SPEC bin=16 双锁）

* [x] 步骤3：吹气/噪声 10s：记录 13 维（期望与静音/纯音明显不同）→ §3.5 ✅（c1 符号反转 + 宽带 BANDS + MEL 峰 b=23 vs 纯音 b=7）

* [x] 步骤4：三种信号两两差异明显 → ✅（§3.3\~3.5 交叉证据：c0 -620/-3480/-860 + 谱形分布三项独立差异）

* [x] 步骤5：运行 `./scripts/ci_local.sh`，输出报告 → 待本阶段末尾记录（见执行日志）

* **判定标准**：AC-01/AC-02 有实测证据（记录实际值才算通过）✅（§3.3\~3.5 数值齐）

* **⏸️ 停等：人确认后进入阶段 5** ✅（阶段 1\~4 期间人逐项确认："认可影响范围判定，执行 e 钳位顺手项" / "认可方案 A'" / "好的，执行吧"）

\===== 切换至 Review Agent =====

### 🔴 阶段 5：审查（角色：Review Agent）✅ 完成（2026-08-30，R23 独立评审：Ready with fixes → 全部修复）

* **阶段目标**：独立审查——AC 逐条打勾、验证结论回填

* 评审独立复核证据：e 钳位非负性论证成立（acc=Σw² 恒非负）+ 溢出可达性复算（上界 4.17e11≫INT32\_MAX）+ INT32\_MIN 边界覆盖 + VCVT 饱和语义新旧固件等价 + energy\_x100 唯一消费者 printf（与 13 维路径无交集）+ 三态数值与存档载体逐值吻合零篡改 + 安全红线未触碰。

* Issues 处置：**I-1** 出处声明失实（A3-03 §3.3 实为空模板）→ 已更正为真实存档载体口径；**I-2** A3-03 文档第 8 次覆写残留 → git restore 闭环；**I-3** AC-10 证据口径 → 补"基线固件"限定+冒烟覆盖面明示；**M-1** 45262/45760 帧间波动 → 已注；**M-2** 阶段 4 停等痕迹 → 已补；**M-3** §5.1 影响文件 → 改实际清单；**M-4** AC-05 勾选时点 → 已注。

* 全部通过 → ✅ 回填「维护与调试」+ 父FEAT项目表 + INDEX + 维护地图

## 3. 傻瓜式操作单（照做即可）

### 3.0 物品/工具清单（动手前先打勾，缺一不可）

* [x] 核心板（A1/A2/A3-01\~03 链路已验证通过）

* [x] SSCOM 串口助手（115200，COM 以设备管理器为准）

* [x] USB-TTL（CH340）

* [x] ST-Link（SWD 四线；仅当需要补打印重烧时用）→ 本 FEAT 用于 e 钳位烧录

* [x] 已知声源：1kHz 正弦（手机 APP / 信号发生器，A2-02 同款）

* [x] 5V 电源：电脑 USB 口或非快充 5V 适配器（**供电红线：严禁手机快充头**）

### 3.1 编译烧录（仅当需补打印代码时执行；插拔 ST-Link 前先断电源）

1. 运行 `firmware\build.bat` → 期望无错误退出、无新增警告。（记录：**Build OK，零警告**；本 FEAT 唯一代码改动=e 钳位 1 行，阶段 3 实测）
2. 接好 ST-Link 四线，运行 `firmware\build_and_flash.bat` → 期望 `wrote ... bytes` / `Verified OK`。（记录：**Verified OK**，openocd；text 166840→166860（+20B））
3. 失败兜底：OpenOCD 连不上 → 查四线/驱动；仍不行 → `firmware\flash_isp.bat`（BOOT0+USART1，HANDOFF §4.3）。（未触发）
4. 固件已有 13 维打印 → 本步跳过，直接到 3.2。（✅ A3-03 已实现 `MFCC13 c=...` 打印，零补打印代码）

### 3.2 准备（阶段 4 执行）

1. USB-TTL 接核心板：TX↔RX **交叉**、GND 共地；SSCOM 打开设备管理器里的 COM 口，波特率 **115200**。
2. 按复位键，期望依次出现三行：`SoundDog boot OK` → `I2S_DRV_Init ret=0` → `I2S DMA started`。（实测 ✅ boot OK + OLED init + FFT selftest peak bin=16）
3. 确认收到 13 维打印行（格式：**待定（需人确认）**；记录实际格式：___），维度 = 13（记录：___ 维）。（实测 ✅ **格式=`MFCC13 c=<13 维 ×100 整数逗号串>`** **1s 节流**（A3-03 已定稿，本 FEAT 阶段 2 确认沿用）；**维度=13** 全程无 NaN/INF）

> **数据口径（阶段 2 定稿，方案 A'）**：三态数据复用 **A3-03 阶段 4 用户亲测三态**（固件差异仅 e 钳位 1 行防护性代码，不参与 13 维算法路径，VCVT 饱和语义下新旧固件 e 输出等价）。**存档载体**：`scripts/a3_03_pc_check.py` 固件帧数组（每态 1 帧可溯源）+ A3-03 执行日志概括值；§3.3\~3.5 每态 5 行采样中其余行来自用户当时粘贴的串口会话原文（本文件执行日志与 A3-04 会话记录内，未另存原始日志文件）——出处口径已按评审 I-1 更正。e 钳位单独以 8s 冒烟验证（boot/节拍/安静态 e=224455 正常、钳位未误触发）。

### 3.3 测试 1：静音（10 秒）

1. 保持环境安静 10 秒，抄录 5 行 13 维（记录数值：\_\_\_）。
2. 期望：13 维整体能量低、无 NaN/INF、数值稳定。
   → **实测 ✅（复用 A3-03 17:45:14\~17:45:18 安静态日志）**：5 行采样 c0 ∈ {-823,-640,-620,-623,-686}，c1 ∈ \[-255,-213]，其余维 |c|<135；无 NaN/INF；f=100 帧/s 稳定。（注：环境为普通室内非消声室，"整体能量低"按相对口径判——安静态 c0 ≈-620 vs 1kHz -3480，可分）

### 3.4 测试 2：1kHz 纯音（10 秒）

1. 用声源播放 1kHz 正弦 10 秒（音量适中，A2-02 曾验证过该声源），抄录 5 行 13 维（记录数值：\_\_\_）。
2. 期望：低频带对应系数明显大于静音、数值稳定、无 NaN。
   → **实测 ✅（复用 A3-03 17:45:22\~17:45:27 1kHz 日志）**：5 行 c0 ∈ {-3480,-3486,-3528,-3471,-3439}（标准差 ~~30，极稳定），c1≈+26~~39；MEL **b=7=45760** 精确命中（低频带能量集中；A3-03 日志另帧记录 45262，帧间正常波动——M-1 注）+ SPEC peak\_bin=16(1000Hz) ratio=62 交叉锁定；无 NaN。

### 3.5 测试 3：吹气 / 宽带噪声（10 秒）

1. 对麦克风吹气 10 秒（或播放宽带噪声），抄录 5 行 13 维（记录数值：\_\_\_）。
2. 期望：高频带分量出现，与纯音分布**明显不同**。
   → **实测 ✅（复用 A3-03 17:45:31\~17:45:37 吹气日志）**：c0=-860、c1=-318、c2=-135（与纯音 c1=+26 **符号反转**）；BANDS 宽频 7469,5637,1738,902,889...（高频带分量明显，纯音集中 b=7 一带）；MEL 峰 b=23/24（纯音 b=7）；音量不变性实证（MEL 千倍幅度 MFCC 同量级）；无 NaN。

### 3.7 补充：e 钳位冒烟（本 FEAT 唯一代码改动验证）

→ **实测 ✅（2026-08-30 烧录新固件后 8s，serial\_capture 全文观察）**：boot 三行 + MFCC13/MEL/MFCC f 节拍齐 + **SPEC/BANDS/OLED init 行同屏正常**；安静态 e=224455/13848/10669 正常值（**钳位未误触发**）；c0≈-689 与基线一致（无回归）。饱和路径（吹气 e=INT32\_MAX）以代码审查判定（防护性 1 行，负值仅溢出产生；评审复算：能量上界 4.17e11≫INT32\_MAX 可达，VCVT 正溢出本就饱和 INT32\_MAX 与钳位目标同值）——R25 口径：未上板复测饱和路径，判据=审查+未误触发。

### 3.6 故障速查表

| 现象            | 可能原因                      | 处理                              |
| ------------- | ------------------------- | ------------------------------- |
| 13 维恒为 0      | A3-03 输出全 0，链路断裂          | 回溯 A3-03 / A3-02 / A3-01 各段打印定位 |
| 静音与吹气差异小      | 环境噪声大 / 增益过高 / 麦克风问题      | 换安静环境；核对 A1 采集（A1-02 max 变化）    |
| 1kHz 纯音无反应    | 声源频率/音量/连接问题              | 换扬声器/提高音量；确认 A2-02 曾用该声源验证通过    |
| 打印乱码          | 波特率 / TX↔RX 未交叉 / GND 未共地 | 改 115200；交叉接；共地                 |
| 数值含 NaN / INF | A3-03 Log 防护缺失            | 退回 A3-03 处理                     |
| 三种信号两两差异都不明显  | 数值范围异常（如溢出饱和）             | 记录原始值，退回 A3-02/A3-03 核对算法参数     |

## 4. 验收标准（12 条）

### 功能验收

* [x] AC-01 串口打印 13 维 MFCC，维度恒为 13、无 NaN/INF（记录于 §3.2）✅ 2026-08-30：MFCC13 行全程 13 维无 NaN/INF（§3.2/§3.3\~3.5）

* [x] AC-02 静音 / 1kHz 纯音 / 吹气三种信号 13 维两两差异明显，与已知信号特征吻合（记录实测值，§3.3\~§3.5）✅ 2026-08-30：c0 -620/-3480/-860 + c1 符号反转 + MEL b=7 vs b=23 三重差异；1kHz 低频带命中（b=7+SPEC bin=16）

### 代码质量

* [x] AC-03 若改代码：编译通过（`build.bat` 无错误退出）✅ Build OK（e 钳位 1 行）

* [x] AC-04 若改代码：无新增警告（记录警告数）✅ 零警告

* [x] AC-05 文档已更新：验证结论回填本 FEAT §5 维护章节 + 父FEAT项目表文件列回填 ✅ 本阶段 5 收官时回填（M-4 注：勾选时点=收官提交前）

### 安全红线

* [x] AC-06 无硬编码密钥 —— **N/A**（嵌入式代码，无密钥/凭据概念）

* [x] AC-07 错误处理完善：打印缓冲/格式无越界（边界条件下不崩溃）；采集异常（无数据）可识别 ✅ 沿用 A3-03 I-1 钳位 + s\_mel\_ok 门控（13 维恒 0 行已禁）；e 钳位补能量饱和 UB 防护

* [x] AC-08 用户输入已校验 —— **N/A**（无外部用户输入；声源为物理信号）

### 测试与边界

* [x] AC-09 新增测试自动运行 —— **N/A**（项目无自动化测试框架，验证走 §3 串口采集）

* [x] AC-10 回归：A1-02 / A2-02 / A3-01\~03 输出不被破坏 ✅ e 钳位新固件 8s 冒烟（boot/节拍/c0 基线/e 正常/SPEC/BANDS/OLED 同屏，§3.7）；三态数据为 A3-03 基线固件证据（I-3 限定：VCVT 饱和语义下新旧固件 e 等价，评审核实）

* [x] AC-11 测试覆盖率 ≥80% —— **N/A**（无覆盖率工具）

* [x] AC-12 按 FEAT 模板执行：5 阶段按序、角色声明、⏸️ 停等、日志完整 ✅ 1\~4 阶段每步停等人确认（"认可/执行/好的执行吧"）

## 5. 维护与调试（阶段5通过后回填）

### 5.1 影响文件

| 文件                             | 改动类型    | 说明                                                                                                                    |
| ------------------------------ | ------- | --------------------------------------------------------------------------------------------------------------------- |
| `firmware/soundDog/BSP/mfcc.c` | 修改（3 行） | e 钳位：energy\_x100 float→int32 溢出 UB 防护（A3-01 评审 Minor#2 清偿；统计打印字段，非算法参数——M-3：实际路径 BSP/ 非 Src/dsp/，§1.3 字面范围已由人明示授权扩展） |
| `firmware/soundDog/Makefile`   | 无改动     | 无新源文件                                                                                                                 |
| （本 FEAT §5.3）                  | —       | 验证结论：三种信号实测 13 维记录（§3.3\~3.5）                                                                                         |

### 5.2 调试要点

* 差异不明显 → 环境噪声/增益/数值范围（记录原始值，必要时退回 A3-02/A3-03）

* 无打印 → 串口接线/波特率/打印代码缺失

* e=INT32\_MAX → 输入爆表（钳位语义化输出；核查麦克风增益/物理削顶 max=32768）

### 5.3 测试入口

* SSCOM 115200 采集三种信号（对应 AC-01/AC-02）；声源 = 1kHz 正弦（A2-02 同款）

* **验证结论（阶段 5 回填，2026-08-30）**：三态 13 维两两差异明显且与信号特征吻合（安静 c0≈-620 低谱 / 1kHz c0≈-3480 低频带 b=7 命中 / 吹气 c0≈-860 宽带 c1 符号反转）——**A4 模版比对输入可信**。13 维基线值供 A4-01 参考（×100 定标，float 原值在 s\_mfcc13）。

## 6. 执行日志

| 时间         | 阶段      | 角色     | 摘要                                                                                                                                                                                                                                                                                                                          | 遇阻                                                     |
| ---------- | ------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 2026-08-30 | 阶段 1 准备 | Dev    | A3-03 🟢 依赖确认；打印现状=MFCC13 行已就位（A3-03 实现）→ **纯验证 FEAT 零补打印**；打印节流"待定"项已被上游闭环（1s）。影响范围：无算法改动 + e 钳位顺手项（A3-01 Minor#2 第三次出现，利害分析后人确认执行）                                                                                                                                                                                        | 无                                                      |
| 2026-08-30 | 阶段 2 设计 | Dev    | 方案 A'：三态数据复用 A3-03 人亲测基线（固件差异仅 e 钳位 1 行防护性代码，不参与 13 维算法路径）+ e 钳位单独冒烟验证。§3 操作单定稿（格式/节流引用 A3-03）。用户质疑重复采集后确认免除（A3-03 日志即完整证据主体）                                                                                                                                                                                               | 初版设计重复安排采集，用户质疑后修正为复用口径                                |
| 2026-08-30 | 阶段 3 实现 | Dev    | e 钳位落地（mfcc.c:103-105，1 行防护）；Build OK 零警告；烧录 Verified OK；text 166840→166860（+20B）；8s 冒烟：boot 三行 + 节拍齐 + e=224455 正常（钳位未误触发）+ c0≈-689 基线一致                                                                                                                                                                                   | 无                                                      |
| 2026-08-30 | 阶段 4 测试 | Test   | 方案 A' 口径：三态 §3.3\~3.5 回填（安静 c0≈-620 / 1kHz c0≈-3480 稳定+MEL b=7 命中 / 吹气 c0≈-860+c1 符号反转+宽带）；三重差异判据 ✅；e 钳位冒烟 §3.7 ✅（R25 口径：饱和路径未上板复测，判据=代码审查+未误触发）；ci\_local **全量 CI 通过**                                                                                                                                                     | 文档覆写第 6/7 次（A3-03 表格重排 + 本文件 `-`→`*`），防御流程 Edit 后即查即恢复 |
| 2026-08-30 | 阶段 5 审查 | Review | R23 独立评审（subagent 只读约束，A3-03 归因修正落地措施）：**Ready with fixes**——独立复算 e 钳位非负性/溢出可达性/VCVT 等价性/消费者隔离全成立；三态数值与 a3\_03\_pc\_check.py 逐值吻合零篡改。I-1 出处声明失实（引用了空模板 §3.3）→已更正真实载体；I-2 第 8 次覆写残留→restore；I-3/M-1\~M-4 全部修复。**A4 立项输入**：① 13 维 float32 s\_mfcc13 输入可信（三态基线值 §5.3）② e 钳位已闭环（A3-01 Minor#2 不再遗留）③ 谱形 c1 可作强区分特征（纯音+/噪声-符号反转） | 覆写第 9 次（收官状态行 `-`→`*`，即查即修）                            |

