# FEAT-A4-模版比对检测（父FEAT · 统筹规划）

> **AI 执行规则**：父FEAT 只做统筹规划，禁止编写实现代码；实现一律拆成子FEAT。
> 创建/修改项目表后必须停等，输出结果等人类确认方向后才允许创建子FEAT。
> 禁止自审自判完成：父FEAT 置 🟢 仅当项目表全部子FEAT 🟢 且每个都经过 Review。
> 历史记录只更新状态字段，严禁删除/篡改已完成记录。

## 0. 元数据
- **角色**：Planner（统筹规划）
- **优先级**：P1
- **预估总耗时**：8h
- **当前状态**：🟡进行中（A4-01 已收官 2026-09-01；**A4-02 已收官 2026-09-01**：dist 模块欧氏 d_min 挂 mel\_frame\_cb 100 帧/s，金属敲击判别比 2.52× 达标、吹气类不可分已归因（宽带噪声与背景同分布，异常类型边界交 A4-03）；**A4-03 已收官 2026-09-01**：连续帧防抖双轨状态机（连续30帧|滑窗30/100→报警，连续60帧→解除），阶段5 R23 独立评审通过；A4-04 未开始）

## 1. 总体目标与方向

### 1.1 核心目标（一句话）
> 用正常模版库对 MFCC 特征做距离比对，实现异常检测判决（含防抖）。

### 1.2 大方向策略
> 先离线生成模版，再实时距离比对，最后加连续帧防抖与可调参数。

### 1.3 硬边界（继承 CLAUDE.md）
- [x] 是否涉及底层库 / 第三方库？→ 否
- [x] 否 → 继续

## 2. 项目表（子FEAT清单）

> 每个子FEAT 按子模板（`.template-child.md`）创建。子FEAT 文档待细化时再建。

| 子FEAT编号 | 功能 | 验收要点 | 优先级 | 依赖 | 状态 | 文件 |
|-----------|------|----------|--------|------|------|------|
| FEAT-A4-01 | 离线模版生成 | PC 端 K-means(K=16) 生成正常模版 | P1 | 依赖 A3 | 🟢已完成 2026-09-01 | `FEAT-A4-01-离线模版生成.md` |
| FEAT-A4-02 | 距离实时比对 | 欧氏/余弦距离，取最小 d_min | P1 | 依赖 01 | 🟢已收官 2026-09-01 | `FEAT-A4-02-距离实时比对.md` |
| FEAT-A4-03 | 连续帧防抖 | 连续 30 帧异常报警、60 帧正常解除 | P1 | 依赖 02 | 🟢已完成 | `FEAT-A4-03-连续帧防抖.md` |
| FEAT-A4-04 | 参数可调 | 阈值/帧数可经 RS485 下发 | P2 | 依赖 03 | 🔴未开始 | `FEAT-A4-04-参数可调.md` |

> **A4-03 立项输入（A4-02 阶段 4 R27 补研结论，2026-09-01）**：判决统计量不裸用 1s 窗 max 过阈值（ViolaWake PR#34 实证 max 对抽稀敏感且单帧尖峰即误报）；业界一致做法为窗口均值（Edge Impulse 官方后处理）或 m-of-n 连续帧比率（prmon 实例：≥10% 时间步被标记才判异常）——"连续 30 帧异常"规划本身即 m-of-n 形态，另需考虑窗口均值与计数双口径（AdaBEAM mean/max 双轨先例）。

> 子FEAT 完成后回填本行状态与文件链接。

## 3. 依赖与执行顺序

1. 01 模版 → 02 比对 → 03 防抖 → 04 参数下发（串行）

## 4. 阻塞与下一步

- **阻塞**：无（A3 已全线收官；A4-01/A4-02 已收官）
- **下一步**：开 A4-03 连续帧防抖（输入已就绪：d\_min 100 帧/s 实时流 + 判别实测区间正常 1.29\~6.96/金属 1.80\~12.69；立项输入三条见 §3 备注 + A4-02 评审余留观察项）

## 5. 完成定义（DoD）

- [ ] 项目表全部子FEAT 🟢 且每个经 Review
- [ ] 本表状态与 `docs/features/INDEX.md` 一致
- [ ] 经验教训已提炼（如有）

## 6. 执行日志

| 时间 | 动作 | 角色 | 摘要 | 遇阻 |
|------|------|------|------|------|
| 2026-09-01 | A4-01 收官 | Dev/Review | 600 帧 0 丢弃 → K-means(K=16, seed=42) 16 簇全非空 → `model_normal.c/h` 编译期常量表（MODEL\_NORMAL\_K/D 接口，A7-03/A4-04 就绪后换数据来源不改语义）；R23 独立评审 2 项重要问题修复闭环，12 AC 全过。移交 A4-02：模版偏移风险（抖音模拟声 vs 真实部署）与阈值调优联动 | 无 |
| 2026-09-01 | A4-02 收官 | Dev/Review | dist.c/h 欧氏（平方比较+1 次 sqrt）挂 mel\_frame\_cb 100 帧/s + DMIN d/m 双口径打印；size +1132B（模表 gc 保留验证）；实测金属敲击判别比 2.52× 达标（吹气类 1.4× 不可分，归因宽带噪声与背景同分布，异常类型边界交 A4-03）；PC 离线复算固件正确性闭环；R23 独立评审零代码缺陷，I/M 7 项修复闭环。移交 A4-03：阈值区间输入 + 帧号对齐/错位行 caveat/异常类型边界三条立项输入 | 无 |
