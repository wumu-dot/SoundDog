# 经验教训提炼

> 从 `docs/troubleshooting/bug_shturl` 累计 ≥5 条故障记录后，
> 提炼通用规范写入此文件，避免同类问题重复发生。

## 提炼规则

（暂无 — 项目启动中，bug_shturl 累计 < 5 条）

## 已收录案例索引

| 日期 | 案例 | 核心教训 |
|------|------|---------|
| 2026-08-28 | [BUG-002 I2S 数据提取问题详解](BUG-002-I2S数据提取问题详解.md) | ① 验证信号链必须验证"因果"（扰动输入看输出），不能只看"数值非零"；② 器件损坏期间得出的软件验证结论必须标"待复核"；③ 位对齐问题先打 RAW dump 再写提取代码；④ max/min 极值统计对污染样本零抵抗力，调试期多统计量交叉印证；⑤ 外设数据手册槽格式必须与主控字格式逐 bit 对表，相位关系必须实测；⑥ **调试外设问题前先查 `外部资源索引.md` 和预研文档**——BUG-002 的答案早就写在 CubeMX_I2S3配置详解.md §0.4 的时序图里，却因隧道视野被绕过 12 天（已固化为 R26） |
| 2026-08-29 | BUG-20260829-001 频谱 unsigned 除法陷阱 | ① **宏带 `u` 后缀（如 `256u`）与可能为负的有符号变量混算（除/模），负值隐式转 unsigned**——去直流均值/移动平均/差分等可能为负的中间量，运算前必须显式 `(int32_t)` cast 或改 float 域计算；② 数值"冻结不变"而输入在变 = 计算路径某层与输入脱钩，先查类型转换再查数据链；③ 诊断技巧：打印值与真实值呈 2^n 偏移（如 +2^24）时直接锁定 unsigned 转换点，一轮快照即可确诊；④ A3 分帧/预加重/差分代码将大量出现同类运算，编码前先对照本条 |
| 2026-08-29 | BUG-20260829-003/004 串口交错 + boot 契约字符串破坏 | ① ISR 上下文禁止 printf（阻塞 + `HAL_UART_Transmit` 不可重入双重违规），打印一律移到任务侧消费点，全固件同一时刻只允许一个上下文写 USART1；② **printf 字符串是项目契约不是自由文本**——改任何打印串前先 grep 代码 + docs/ 双侧（"I2S DMA started" 被 ~11 个 FEAT 文档当验收判据引用）；③ R9 影响地图对"一行级小改"同样适用，本轮正是跳过确认才漏掉契约面 |
| 2026-08-29 | BUG-20260829-005/006 OLED 乱码→黑屏（四轮弯路后 GDB 一轮定位） | ① **FreeRTOS CM4F：osKernelStart 之前调用任何内核 API（xQueueCreate/xTaskCreate 等，走 pvPortMalloc→临界区），BASEPRI 被抬到 0x50 后永不落下**（uxCriticalNesting 哨兵 0xaaaaaaaa 机制）→ SysTick 全灭 → 预调度器期的 HAL_Delay 死循环。规则：依赖 tick/中断的初始化必须放在首个内核调用**之前**；② **"串口无声"≠死机≠外设坏——先 GDB 挂接看调用栈 + basepri + uwTick 再动手**（本轮前四轮押错层：字符串→参数→接线→电源，真因是调度器时序）；③ I2C 从机对语义错误静默 ACK（HAL_OK≠屏幕收到正确语义），外设帧格式必须对照器件手册逐字段核对；④ **跨硬件抄开源代码分层核对（R28）：协议/流程/帧格式可照抄，电气参数（0xD5/0xD9/0xDB/0x81）是原作者调优值，必须用本硬件实测过的来源**；⑤ 全屏白 2s 诊断期设计值得复用：上电首帧全亮直接区分"init/硬件层 vs 刷新/绘制层"故障 |
| 2026-08-30 | BUG-20260830-001 调试器停机事故（"挂死"误诊链） | ① **gdb detach ≠ resume**：OpenOCD 后端 connect 即 halt、detach 不恢复——凡 gdb batch 会话必须以 `monitor resume`/`reset run` 收尾；② **"挂死"先查调试器再怀疑固件**：被外部 halt 的 CPU 与固件死锁表现完全一致（tick 冻结/中断挂起/串口静默），先读 DHCSR 看 C_HALT/S_HALT；③ **运行态监控禁用 gdb connect**（connect 即停机），改用 telnet `mdw` 直读（.skills/oocd_probe.ps1）；④ **GDB 读外设寄存器可能同步失真**（SysTick CTRL 读 0 实为 7），关键寄存器用 OpenOCD mdw 直读交叉验证；⑤ 用户报"复位"先问"是否有人烧录/复位"再排查 brown-out——本次及此前 3 次"自发复位"全部是调试动作 |

---

*状态：🟡 案例收录中（正式提炼规则待 bug 累计 ≥5 条激活）*
