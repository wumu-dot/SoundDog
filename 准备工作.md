# SoundDog — 工业异响音频检测节点 软件开发指南

> *"Like a watchdog, but it listens."*
>
> 基于 STM32F407 的分布式工业音频异常检测系统
> 当前阶段：**传统 DSP（MFCC + 阈值）**，未来可升级 CMSIS-NN 轻量模型

---

## 目录

- [1. 软件开发环境准备](#1-软件开发环境准备)
- [2. 嵌入式开发标准流程](#2-嵌入式开发标准流程)
- [3. 调试方法论](#3-调试方法论)
- [4. 建议的开发节奏](#4-建议的开发节奏)
- [5. 现在可以立即开始的事](#5-现在可以立即开始的事)

---

## 1. 软件开发环境准备

### 1.1 工具链安装

| 工具 | 用途 | 获取方式 |
|------|------|---------|
| **STM32CubeIDE** | STM32 集成开发环境（含 CubeMX 配置 + 编译 + 调试） | ST 官网免费下载 |
| **STM32CubeMX**（独立版） | 引脚配置、时钟树、外设初始化代码生成 | 同上，或 IDE 内集成 |
| **ARM CMSIS-DSP 库** | FFT、矩阵运算、DCT 等 DSP 函数 | CubeMX 中勾选启用 |
| **串口调试助手** | 查看调试日志、频谱数据 | 任意一款（推荐 MobaXterm / SSCOM） |
| **Python 3.9+** | PC 上位机、校准工具脚本 | [python.org](https://www.python.org) |
| **虚拟串口/RS485 测试工具** | 协议验证 | 可以用 Python `pyserial` 先模拟 |

### 1.2 STM32 固件库准备

在 CubeMX 中创建项目时勾选这些中间件：

| 组件 | 说明 |
|------|------|
| **CMSIS → DSP** | **最重要**，FFT / MFCC 全靠它 |
| **FreeRTOS** | 任务调度框架 |
| **HAL 驱动** | I2S、I2C、USART、SPI、DMA、GPIO、TIM（PWM） |

### 1.3 PC 上位机环境

```bash
# 安装 Python 依赖
pip install pyserial numpy matplotlib PyQt5 scikit-learn
```

---

## 2. 嵌入式开发标准流程

> **核心原则：一次只验证一个外设，通过后再叠加。**

---

### 第一步：硬件基础驱动验证（Week 1 前半段）

```
目标：确保每个外设都能单独工作，不要上业务逻辑
```

| 序号 | 验证项 | 方法 | 通过标志 |
|:----:|--------|------|---------|
| 1 | **调试串口** | `printf()` 输出 "Hello SoundDog" | 串口助手收到 |
| 2 | **LED + 蜂鸣器** | GPIO 翻转，PWM 调亮度 | 肉眼可见 |
| 3 | **I2C OLED** | 显示一行文字 | OLED 有显示 |
| 4 | **I2C SHT30** | 读寄存器，打印温度 | 数值合理 |
| 5 | **I2S + INMP441** | DMA 采集，打印原始 PDM 数据 | 有数据流 |
| 6 | **RS485 + MAX3485** | 发送一帧，PC 端接收 | 串口助手收到 |

---

### 第二步：音频采集链路打通（对应 A1）

> **⚠️ 这是整个项目风险最高的一环，优先级调最高。**

```
开发顺序：

CubeMX 配置 I2S3 + DMA（16kHz, 16bit, 双缓冲）
        │
        ▼
实现 PDM → PCM 软件抽取（CIC 滤波器）
        │
        ▼
用 DMA 传输完成中断切换 ping/pong 缓冲区
        │
        ▼
串口打印 PCM 原始值，用 Python matplotlib 画波形验证
```

**PC 端波形验证脚本示例：**

```python
import serial
import numpy as np
import matplotlib.pyplot as plt

ser = serial.Serial('COM7', 921600)
data = []

for _ in range(256):
    val = int(ser.readline().decode().strip())
    data.append(val)

plt.plot(data)
plt.title("PCM Waveform from STM32")
plt.xlabel("Sample Index")
plt.ylabel("Amplitude")
plt.show()
# ✅ 应该看到类正弦波形
```

---

### 第三步：DSP 流水线逐步搭建（对应 A2 → A3 → A4）

> **每步都要有可验证的输出，不要一上来就写完整 pipeline。**

#### A2 阶段 — FFT + 频谱可视化

```
256点 FFT
    │
    ▼
打印 128 个幅度值
    │
    ▼
Python 画频谱图（确认频域信息正确 → 播放固定频率音叉测试）
    │
    ▼
32 维 Mel 滤波
    │
    ▼
打印 32 个能量值 → 柱状图显示在 OLED
```

#### A3 阶段 — MFCC 特征提取

```
Log + DCT
    │
    ▼
打印 13 维 MFCC
    │
    ▼
确认数值范围合理（通常在 -20 ~ +20）
```

#### A4 阶段 — 模版比对 + 阈值检测

```
Python 脚本离线训练出 16 个模版向量
    │
    ▼
硬编码到 STM32 的数组里
    │
    ▼
实现欧氏距离实时比对
    │
    ▼
串口打印：帧序号 + 最小距离 + 阈值 + 判定结果
```

---

### 第四步：业务逻辑 + 防抖（对应 A4 + A6）

```
检测结果
    │
    ▼
连续帧计数器
    │
    ▼
LED / BUZZER 驱动
    │
    ▼
RS485 上报
```

**防抖逻辑用状态机实现**，不要用 if-else 堆叠：

| 状态 | 含义 | 进入条件 |
|------|------|---------|
| `NORMAL` | 正常 | 距离 < 阈值 |
| `WARNING` | 警告（过渡态） | 连续异常帧 > 10 但 < 30 |
| `ALARM` | 报警 | 连续异常帧 ≥ 30 |

- 状态转移条件清晰，便于调试
- 报警解除需连续正常 ≥ 60 帧

---

### 第五步：RS485 协议栈（对应 A5）

> **先单节点，再多节点。**

```
阶段 1：STM32 与 PC 直连
  USART3 → 串口线 → PC
  验证帧打包/解析/CRC16 正确
        │
        ▼
阶段 2：加上 MAX3485 模块
  验证 DE/RE 引脚方向控制
        │
        ▼
阶段 3：面包板挂两个节点（STM32 + ESP32）
  PC 轮询地址 0x01 和 0x02
  验证无冲突
```

---

### 第六步：PC 上位机最小可用版本（Phase B 的 B1）

> 不追求美观，先实现核心功能。

```
✅ 串口连接（选择 COM 口、波特率）
✅ 接收并解析协议帧
✅ 实时滚动显示检测结果（距离值、阈值、状态）
✅ 一键发送"校准开始"指令
✅ 保存收到的频谱数据到 CSV
```

---

## 3. 调试方法论

### 3.1 分层调试原则

| 层 | 调试手段 | 工具 |
|----|---------|------|
| **外设驱动** | GPIO toggle 示波器看时序 | 逻辑分析仪（低成本 Saleae 克隆版，~30元） |
| **数据流** | 串口打印关键变量 | `printf()` + 串口助手 |
| **算法** | 导出数据到 PC，Python 离线跑 | numpy / matplotlib |
| **系统级** | FreeRTOS 任务统计 | `vTaskList()` 打印 CPU 占用 |
| **通信** | RS485 监听器（并线收数据） | 两个 USB 转 485 模块 |

---

### 3.2 关键调试技巧

#### 技巧 1：用 GPIO + 示波器测量函数耗时

```c
GPIO_SetHigh();   // 拉高 GPIO
run_fft();        // 执行 FFT
GPIO_SetLow();    // 拉低 GPIO
// 示波器量脉冲宽度 = FFT 耗时
```

#### 技巧 2：MFCC 交叉验证

```
STM32 算完 13 维 MFCC → 串口打印
Python 用 librosa 算同一段音频的 MFCC → 对比
误差 < 5% 即为通过 ✅
```

#### 技巧 3：模版阈值验证

```
录制正常音频 wav → Python 回放 → STM32 麦克风采集
对比检测距离是否稳定
```

#### 技巧 4：RS485 总线监听

```
用两个 USB 转 485 模块：
  模块A：PC 主站，正常收发
  模块B：只收不发，并线监听总线数据
可以观察到总线上所有帧，排查冲突/丢帧
```

---

### 3.3 常见问题排查表

| 现象 | 可能原因 | 排查方法 |
|------|---------|---------|
| I2S 无数据 | 时钟配置错误 / 引脚 AF 不对 | 逻辑分析仪看 BCK/WS 有无波形 |
| DMA 丢帧 | 缓冲区太小 / 中断优先级低 | 打印双缓冲序号，看是否连续 |
| FFT 结果异常 | 数据类型不对 / 窗函数忘加 | Python 离线跑同一数据对比 |
| MFCC 值全是 0 | Mel 滤波器频率映射错误 | 逐个打印滤波器组输出 |
| RS485 收发乱码 | 波特率不匹配 / DE 切换太慢 | 降低波特率到 9600 先验证 |
| 误报太多 | 阈值太严格 | 增大阈值倍数（3σ → 5σ） |
| 漏报太多 | 阈值太宽松 | 减小阈值倍数（5σ → 2σ） |

---

## 4. 建议的开发节奏

```
📅 第 1 周：环境搭建 + CubeMX 配置 + 外设逐个点亮
  ├── Day 1-2: 工具链安装，固件框架搭建
  ├── Day 3-4: I2S 音频采集链路（⚠️ 最高优先级）
  └── Day 5-7: OLED、SHT30、LED、蜂鸣器外设验证

📅 第 2 周：DSP 流水线
  ├── Day 1-2: FFT + 频谱显示
  ├── Day 3-4: MFCC 特征提取 + Python 对比验证
  └── Day 5-7: 模版比对 + 防抖逻辑 + RS485 通信

📅 第 3 周：系统联调
  ├── Day 1-3: 端到端测试（播放声音 → 检测 → 报警）
  ├── Day 4-5: 多节点 RS485 组网测试
  └── Day 6-7: 阈值参数优化 + 边界情况测试
```

---

## 5. 现在可以立即开始的事

| 优先级 | 行动项 | 预计耗时 |
|:----:|--------|:--------:|
| 🔴 高 | 安装 STM32CubeIDE，新建 F407 项目，让 LED 闪烁 | 1h |
| 🔴 高 | 用 CubeMX 配置 I2S3（PC10/PA4/PC12），生成初始化代码 | 30min |
| 🔴 高 | 购买逻辑分析仪（淘宝 30 元级别即可），调试 I2S 时序必备 | 下单等快递 |
| 🟡 中 | PC 安装 Python + pyserial + matplotlib | 15min |
| 🟢 低 | 阅读 CMSIS-DSP 文档，熟悉 `arm_rfft_fast_f32` API | 30min |

---

> **一句话总结**：这个项目的技术栈很扎实，最大的风险点在 INMP441 的 **PDM 采集链路**。建议你**先集中精力把音频数据通路打通**，只要能看到正确的 PCM 波形，后面都是确定性的 DSP 运算，基本不会翻车。

---

*需要我进一步展开某个模块的代码细节或 CubeMX 配置截图级说明吗？*